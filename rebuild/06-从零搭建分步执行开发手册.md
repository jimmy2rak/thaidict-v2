# 文档6：从零搭建分步执行开发手册

## 前置要求

- Node.js 18+ / 20+
- npm 或 pnpm
- Git
- Supabase 账号（新建或复用已有项目）
- Vercel 账号
- Brevo 账号（邮件服务，可选）
- AI API 密钥（DeepSeek / OpenAI 等，可选）

---

## 步骤1：项目初始化

### 1.1 创建 Vite + React 项目

```bash
npm create vite@latest thaidict -- --template react
cd thaidict
```

### 1.2 安装依赖

```bash
# 核心依赖（仅5个）
npm install @supabase/supabase-js@^2.49.0 lucide-react@^0.468.0 react@^18.3.1 react-dom@^18.3.1 recharts@^2.15.0

# 开发依赖
npm install -D vite@^6.0.0 @vitejs/plugin-react@^4.3.4
```

### 1.3 配置 vite.config.js

```js
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  server: {
    port: 3000,
    host: true,
  },
  build: {
    outDir: 'dist',
    sourcemap: false,
  },
})
```

### 1.4 配置 vercel.json

```json
{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
```

### 1.5 配置 index.html

```html
<!DOCTYPE html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover" />
    <meta name="theme-color" content="#FAF7F4" />
    <meta name="apple-mobile-web-app-capable" content="yes" />
    <title>词笺 — 中泰双语智能词典</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+SC:wght@300;400;500;600;700&family=Noto+Sans+Thai:wght@300;400;500;600;700&family=Sarabun:ital,wght@0,300;0,400;0,500;0,600;0,700;1,400&display=swap" rel="stylesheet" />
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.jsx"></script>
  </body>
</html>
```

### 1.6 配置 .gitignore

```
node_modules/
dist/
.env
.env.local
.DS_Store
```

---

## 步骤2：Supabase 数据库搭建

### 2.1 创建 Supabase 项目

1. 登录 https://supabase.com → New Project
2. 记录项目 URL 和 anon key（Settings → API）
3. 项目 ID 例如：`zvemahqskgluhirzbcqu`

### 2.2 配置环境变量

在项目根目录创建 `.env` 文件：

```bash
VITE_SUPABASE_URL=https://你的项目ID.supabase.co
VITE_SUPABASE_ANON_KEY=你的anon-public-key
```

### 2.3 创建词典主表

在 Supabase Dashboard → SQL Editor 中运行：

```sql
-- 词典主表（核心数据表）
CREATE TABLE IF NOT EXISTS dictionary_full (
  word TEXT PRIMARY KEY,
  romanization TEXT DEFAULT '',
  romanization_source TEXT DEFAULT 'pending',
  senses JSONB DEFAULT '[]',
  synonyms JSONB DEFAULT '[]',
  antonyms JSONB DEFAULT '[]',
  learner_associations JSONB DEFAULT '[]',
  sources TEXT[] DEFAULT '{}',
  sense_count INT DEFAULT 0,
  enrichment_status TEXT DEFAULT 'pending',
  freq_ttc INT,
  freq_tnc INT,
  freq_phupha INT,
  user_sentence_count INT DEFAULT 0,
  created_at TIMESTAMPTZ DEFAULT now()
);

-- 允许匿名用户读取词典
ALTER TABLE dictionary_full ENABLE ROW LEVEL SECURITY;
CREATE POLICY "anon_read_dictionary_full" ON dictionary_full FOR SELECT USING (true);
```

**senses JSONB 结构：**
```json
[
  {
    "sense_id": 1,
    "pos": "VERB",
    "meaning": "吃；进食",
    "register": "通用",
    "examples": [
      { "thai": "ฉันกินข้าวแล้ว", "zh": "我已经吃饭了" }
    ],
    "segmented": [
      { "text": "ฉัน", "meaning": "我" },
      { "text": "กิน", "meaning": "吃" }
    ],
    "source": "ai"
  }
]
```

### 2.4 创建用户数据表

按顺序运行以下迁移脚本（在 SQL Editor 中）：

**① `20260614_create_user_data_tables.sql`**
创建 8 张用户表：user_bookmarks, user_recent_words, user_folders, user_folder_words, user_learning_plans, user_learning_progress, user_notes, user_settings

**② `phase5_add_rpcs_and_sentences.sql`**
创建 sentences 表 + user_sentence_bookmarks 表 + 搜索 RPC 函数

**③ `20260615_add_sentence_folders.sql`**
创建 user_folder_sentences 表 + create_default_folders RPC

**④ `20260619_add_last_folder_id.sql`**
user_settings 增加 last_folder_id 列

**⑤ `community_words_and_daily_picks.sql`**
创建 community_words 表 + daily_picks 表（v1）

**⑥ `daily_picks_v2.sql`**
替换 daily_picks 为 v2 版本（全局统一，只存ID引用）

**⑦ `create_checkin_tasks.sql`**
创建 user_checkin_tasks + user_checkin_completions 表

**⑧ `migrations/20260616_create_otp_codes.sql`**
创建 otp_codes 表 + 频率限制函数

**⑨ `rls_upgrade_uuid.sql`（最后运行！）**
- TRUNCATE 所有旧数据（Clerk TEXT ID 数据）
- ALTER user_id 列 TEXT → UUID
- 删除所有 anon RLS 策略
- 创建 JWT-based RLS 策略（auth.uid()）

**⚠️ 执行顺序很重要：** 必须先运行 ①-⑧，最后运行 ⑨。⑨ 会清除所有旧数据。

### 2.5 补充创建 user_api_keys 表

迁移脚本中缺少此表的 CREATE TABLE，需手动创建：

```sql
CREATE TABLE IF NOT EXISTS user_api_keys (
  id BIGSERIAL PRIMARY KEY,
  user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  name TEXT NOT NULL DEFAULT '',
  provider TEXT NOT NULL DEFAULT 'openai',
  key TEXT NOT NULL,
  base_url TEXT DEFAULT '',
  model TEXT DEFAULT 'gpt-4o',
  is_active BOOLEAN DEFAULT true,
  created_at TIMESTAMPTZ DEFAULT now()
);
CREATE INDEX IF NOT EXISTS idx_api_keys_user ON user_api_keys(user_id);
ALTER TABLE user_api_keys ENABLE ROW LEVEL SECURITY;

CREATE POLICY "user_select_api_keys" ON user_api_keys FOR SELECT TO authenticated USING (user_id = auth.uid());
CREATE POLICY "user_insert_api_keys" ON user_api_keys FOR INSERT TO authenticated WITH CHECK (user_id = auth.uid());
CREATE POLICY "user_delete_api_keys" ON user_api_keys FOR DELETE TO authenticated USING (user_id = auth.uid());
```

### 2.6 创建 Supabase Storage Bucket

1. Dashboard → Storage → New Bucket
2. 名称：`user-assets`
3. Public bucket：✅ 是（头像需要公开访问）
4. 文件大小限制：5MB
5. 允许的文件类型：image/*

---

## 步骤3：Supabase Auth 配置

### 3.1 启用邮箱密码登录

1. Dashboard → Authentication → Providers
2. Email → Enable ✅
3. 关闭「Confirm email」（推荐，否则需额外处理确认流程）

### 3.2 配置 OAuth（可选）

**Google OAuth：**
1. 前往 Google Cloud Console → Credentials → Create OAuth 2.0 Client
2. Authorized redirect URI：`https://你的项目ID.supabase.co/auth/v1/callback`
3. 在 Supabase Dashboard → Authentication → Providers → Google → 填入 Client ID + Secret

**GitHub OAuth：**
1. GitHub → Settings → Developer settings → OAuth Apps → New OAuth App
2. Authorization callback URL：`https://你的项目ID.supabase.co/auth/v1/callback`
3. 在 Supabase Dashboard → Authentication → Providers → GitHub → 填入 Client ID + Secret

### 3.3 配置 URL 配置

Dashboard → Authentication → URL Configuration：
- Site URL：`https://thaidict.vercel.app`
- Redirect URLs：`https://thaidict.vercel.app/**`

---

## 步骤4：Edge Functions 部署

### 4.1 安装 Supabase CLI

```bash
npm install -g supabase
# 或
brew install supabase/tap/supabase
```

### 4.2 登录 Supabase

```bash
supabase login
supabase link --project-ref 你的项目ID
```

### 4.3 设置 Edge Function Secrets

```bash
# AI 代理
supabase secrets set AI_PROVIDER=deepseek AI_API_KEY=你的密钥 AI_BASE_URL=https://api.deepseek.com AI_MODEL=deepseek-chat

# Brevo 邮件
supabase secrets set BREVO_API_KEY=你的Brevo密钥 BREVO_SENDER_EMAIL=noreply@你的域名.com

# Service Role Key（所有 Edge Function 都需要）
supabase secrets set SUPABASE_SERVICE_ROLE_KEY=你的service-role-key
```

### 4.4 部署 Edge Functions

```bash
# 逐个部署
supabase functions deploy ai-proxy
supabase functions deploy send-otp
supabase functions deploy verify-otp
supabase functions deploy send-reminder
```

### 4.5 验证 Edge Function

```bash
# 测试 ai-proxy
curl -X POST https://你的项目ID.supabase.co/functions/v1/ai-proxy \
  -H "Authorization: Bearer 你的anon-key" \
  -H "Content-Type: application/json" \
  -d '{"prompt": "请为泰语词语\"กิน\"生成词条数据", "provider": "system"}'
```

---

## 步骤5：前端核心代码搭建

### 5.1 创建 CSS 设计系统（src/index.css）

```css
*,*::before,*::after { margin: 0; padding: 0; box-sizing: border-box; }

:root {
  --c-bg: #FAF7F4;
  --c-surface: #FFFFFF;
  --c-p100: #F0EBE3; --c-p200: #E0D5C7; --c-p300: #C4B5A3;
  --c-p500: #8B7355; --c-p600: #5B3A29; --c-p700: #3D261A; --c-p800: #2D2019;
  --c-s100: #F5F5F5; --c-s200: #E8E8E8; --c-s300: #C0C0C0; --c-s500: #808080;
  --c-teal: #5B8C7E; --c-gold: #C4993D; --c-rose: #C45B5B;
  --c-info: #5B7E9E; --c-amber: #D4934D;
  --zh-font: 'Noto Sans SC', -apple-system, sans-serif;
  --th-font: 'Noto Sans Thai', 'Sarabun', sans-serif;
}

[data-theme="dark"] {
  --c-bg: #1A1614;
  --c-surface: #2A2420;
  --c-p800: #F0EBE3;
  /* ... 其他暗色变量 ... */
}

html, body { height: 100%; font-family: var(--zh-font), var(--th-font), sans-serif; background: var(--c-bg); }
#root { max-width: 430px; margin: 0 auto; height: 100dvh; display: flex; flex-direction: column; }
```

### 5.2 创建 Supabase 客户端（src/lib/supabase.js）

```js
import { createClient } from '@supabase/supabase-js'

const supabaseUrl = import.meta.env.VITE_SUPABASE_URL
const supabaseAnonKey = import.meta.env.VITE_SUPABASE_ANON_KEY
export const isSupabaseConfigured = !!(supabaseUrl && supabaseAnonKey)

let supabase = null
if (isSupabaseConfigured) {
  supabase = createClient(supabaseUrl, supabaseAnonKey, {
    global: { headers: { Prefer: 'return=representation' } }
  })
}
export { supabase }
```

### 5.3 创建 Auth Provider（src/main.jsx）

```jsx
import React, { createContext, useContext, useState, useEffect } from 'react'
import ReactDOM from 'react-dom/client'
import App from './App'
import { supabase } from './lib/supabase.js'
import { AppProvider } from './context/AppContext.jsx'
import './index.css'

export const AuthContext = createContext({ user: null, session: null, loading: true, signOut: async () => {} })
export const useAuth = () => useContext(AuthContext)

function AuthProvider({ children }) {
  const [user, setUser] = useState(null)
  const [session, setSession] = useState(null)
  const [loading, setLoading] = useState(true)

  useEffect(() => {
    if (!supabase) { setLoading(false); return }
    supabase.auth.getSession().then(({ data: { session: s } }) => {
      setSession(s); setUser(s?.user ?? null); setLoading(false)
    })
    const { data: { subscription } } = supabase.auth.onAuthStateChange((_event, session) => {
      setSession(session); setUser(session?.user ?? null); setLoading(false)
    })
    return () => subscription.unsubscribe()
  }, [])

  const signOut = async () => {
    if (supabase) await supabase.auth.signOut()
    setSession(null); setUser(null)
  }

  return (
    <AuthContext.Provider value={{ user, session, loading, signOut }}>
      {children}
    </AuthContext.Provider>
  )
}

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <AuthProvider>
      <AppProvider>
        <App />
      </AppProvider>
    </AuthProvider>
  </React.StrictMode>
)
```

### 5.4 创建 AppContext（src/context/AppContext.jsx）

核心状态：
- `userId`, `isLoggedIn`, `authLoading` — 认证状态
- `page`, `setPage`, `visitedPages` — Tab 导航
- `navStack`, `navForward`, `navigateTo`, `goBack`, `goForward`, `resetNav` — overlay 导航栈
- `detailWord`, `unknownWord`, `selectedSentence` — 当前 overlay 数据
- `generatedWords`, `dbWordData` — 词条数据缓存
- `colorMode`, `setColorMode` — 主题
- `handleWordTap` — 统一词条点击处理（查字典 → 有数据进详情，无数据进AI生成）
- `handleGenerated` — AI 生成词条处理

### 5.5 创建 App Shell（src/App.jsx）

核心结构：
1. 未登录 → 显示 `LoginPage` 或 `ResetPasswordPage`
2. 已登录 → 显示 4-tab 主界面 + overlay 层
3. 底部 Tab Bar：首页 / 单词本 / 学习 / 我的
4. 3 层 overlay：UnknownWordPage / WordDetailPage / SentenceDetail
5. 动态 viewport height 适配（`--app-height` CSS 变量）

### 5.6 实现各页面组件

按以下顺序实现：

1. **LoginPage.jsx** — 登录/注册/OAuth/OTP（最先需要）
2. **ResetPasswordPage.jsx** — 3步重置密码
3. **HomePage.jsx** — 搜索 + 每日推荐
4. **WordDetailPage.jsx** — 词条详情 overlay
5. **UnknownWordPage.jsx** — AI 生成 overlay
6. **SentenceDetail.jsx** — 句子详情 overlay
7. **WordBookPage.jsx** — 单词本（4个内部tab）
8. **LearnPage.jsx** — 学习（打卡 + 入口）
9. **ProfilePage.jsx** — 我的（设置 + API + WebDAV）
10. **subsections/** — 各子页面

---

## 步骤6：关键功能实现要点

### 6.1 搜索实现

```js
// 4路并行查询
const [exact, fuzzy, zhSearch, community] = await Promise.all([
  safeQuery(supabase.from('dictionary_full').select('*').eq('word', query).limit(5)),
  safeQuery(supabase.from('dictionary_full').select('*').ilike('word', `%${query}%`).limit(20)),
  safeQuery(supabase.rpc('search_words_zh', { search_term: query, max_results: 20 })),
  safeQuery(supabase.from('community_words').select('*').or(`word.ilike.%${query}%,senses::text.ilike.%${query}%`).limit(20)),
])
// 合并去重 → 排序：精确匹配 > enriched > sense_count > 社区词
```

### 6.2 泰语分词实现

```js
// thaiSegment.js — 最长匹配算法
export function thaiSegment(text) {
  // 1. 遍历文本，尝试匹配 dictWords 中最长的词
  // 2. 未匹配时使用音节启发式拆分
  // 3. 返回 [{ text, meaning }] 数组
}

// 启动时从 DB 加载词典
export async function loadDictFromDB() {
  const words = await loadAllDictionaryWords()  // 分批 1000 条
  dictWords = new Set(words)
}
```

### 6.3 AI 代理调用

```js
// callAiProxy — 通过 Edge Function 代理 AI 请求
export async function callAiProxy(prompt, userApi = null) {
  const fnUrl = `${supabaseUrl}/functions/v1/ai-proxy`
  const { data: { session } } = await supabase.auth.getSession()
  const body = { prompt }
  if (userApi?.key) {
    body.user_api_key = userApi.key
    body.user_base_url = userApi.base_url
    body.user_model = userApi.model
    body.provider = 'user'
  } else {
    body.provider = 'system'
  }
  const res = await fetch(fnUrl, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json', 'Authorization': `Bearer ${session?.access_token}` },
    body: JSON.stringify(body),
  })
  return await res.json()
}
```

### 6.4 CST 时区处理

```js
// 所有日期计算必须使用 CST（UTC+8）
export function getTodayCST() {
  return new Intl.DateTimeFormat('en-CA', { timeZone: 'Asia/Shanghai' }).format(new Date())
}
export function getCSTWeekday() {
  const day = new Date().toLocaleDateString('en-US', { timeZone: 'Asia/Shanghai', weekday: 'short' })
  return { Mon: 1, Tue: 2, Wed: 3, Thu: 4, Fri: 5, Sat: 6, Sun: 7 }[day] || 1
}
```

---

## 步骤7：部署到 Vercel

### 7.1 推送代码到 GitHub

```bash
git init
git add .
git commit -m "feat: initial commit"
git remote add origin git@github.com:你的用户名/thaidict.git
git push -u origin main
```

### 7.2 Vercel 部署

1. 登录 Vercel → New Project → Import Git Repository → 选择 thaidict
2. Framework Preset：`Vite`
3. 环境变量：添加 `VITE_SUPABASE_URL` 和 `VITE_SUPABASE_ANON_KEY`
4. 点击 Deploy

### 7.3 验证部署

1. 访问 `https://thaidict.vercel.app`
2. 检查：登录 → 搜索 → 每日推荐 → 词条详情 → 收藏 → 打卡
3. 检查 Edge Function：AI 生成词条 → 查看 Network 面板确认请求成功

---

## 步骤8：部署后检查清单

- [ ] 邮箱密码登录正常
- [ ] OAuth 登录正常（Google/GitHub）
- [ ] 搜索返回结果（泰语 + 中文搜索）
- [ ] 词条详情显示正常（多义项、例句、近反义词）
- [ ] AI 生成词条正常（未知词 → AI → 社区词库）
- [ ] 收藏/取消收藏正常
- [ ] 文件夹 CRUD 正常
- [ ] 打卡任务 CRUD + 完成/取消正常
- [ ] 每日推荐显示 + 刷新正常
- [ ] 主题切换（light/dark）正常
- [ ] 头像上传正常
- [ ] API 密钥管理正常
- [ ] 学习统计图表正常
- [ ] 笔记 CRUD 正常
- [ ] 句子浏览 + 分词显示正常

---

## 附录：完整迁移脚本执行顺序

```
1. supabase/20260614_create_user_data_tables.sql    — 8张用户表 + anon RLS
2. supabase/phase5_add_rpcs_and_sentences.sql        — sentences + RPC函数
3. supabase/20260615_add_sentence_folders.sql         — 句子文件夹
4. supabase/20260619_add_last_folder_id.sql           — last_folder_id
5. supabase/community_words_and_daily_picks.sql       — 社区词 + daily_picks v1
6. supabase/daily_picks_v2.sql                        — daily_picks v2（替换v1）
7. supabase/create_checkin_tasks.sql                  — 打卡系统
8. supabase/migrations/20260616_create_otp_codes.sql  — OTP表
9. supabase/rls_upgrade_uuid.sql                      — UUID升级 + JWT RLS（最后！）
10. 手动创建 user_api_keys 表                          — 迁移脚本中缺失
11. 手动修复 user_folder_sentences RLS                 — 升级为 JWT（Bug A-1）
12. 手动修复 create_default_folders RPC 参数类型       — TEXT→UUID（Bug A-5）
```
