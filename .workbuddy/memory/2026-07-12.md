# 2026-07-12 工作日志

## 中泰词典（ThaiDict）重建 — Phase 3 完成

完整补全了核心页面与 9 项新功能，本地 mock 数据层下已可跑通（`npm run build` 通过，`npm run dev` 在 http://localhost:3000 正常服务）。

### 本次新增/修复
- **数据层**：新增 `src/lib/db/notes.js`（用户笔记 CRUD），并在 `db/index.js` 导出。
- **主页面**：`WordBookPage`（最近/单词夹/句子夹/单词书 4 Tab + 详情，功能3.8）、`LearnPage`（打卡任务 + 工具入口）、`ProfilePage`（按 G-2 拆为 SettingsSection/ApiKeysSection/WebDavSection/ReminderPage/AchievementsSection 等子组件）。
- **8 个 subsection + 新功能页**：MorphologySection(3.4)、AdjustPlanSection、StatsSection(3.5 recharts 柱状/热力/饼图)、PracticeSection(3.6 题库测验)、DiaryList/Editor/Detail(3.2)、PhrasesSection/PhraseDetailSection、NotesDetailSection/NoteEditorSection、ReminderPage(3.7)。
- **WebDavSection(3.1)**：AES-GCM + PBKDF2 加密密码后存 localStorage，模拟上传。
- **接线**：HomePage「短语库」入口、WordDetailPage「笔记」按钮。
- **关键修复**：subsections 中 `UIComponents` 相对路径应为 `../../components/UIComponents.jsx`；`getGlobal`/`enrichSegmented`/`transformWordData`/`getTodayCST` 实际位于 `mock/store.js` / `utils.js`，不可从 `db/index.js` 导入。

### 架构要点（长期有效）
- 所有页面只依赖 `src/lib/db/*`，由 `isSupabaseConfigured`（`VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY` 是否存在）切换 mock / 真实 Supabase，上线零页面改动。
- 当前本地无 git remote（按计划在阶段6上线时添加），故只 commit 未 push。
- 开发服务器默认端口 3000（vite.config.js 设定）。
- 注意：路径含中文目录「开发者」，Write 易误打成「developer」，每次需核对绝对路径。
