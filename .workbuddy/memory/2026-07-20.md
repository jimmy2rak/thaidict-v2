# 2026-07-20 工作日志

## 修复 /api/send-otp 限频逻辑反转（Brevo 邮件发不出）

### 现象
- 用户反馈：F12 控制台 `POST /api/send-otp 429 (Too Many Requests)`；认为邮件还是「Supabase 自带」。
- 同时控制台有一个 `content_script...startsWith` 报错 —— 那是浏览器扩展（content script），与本项目无关，可忽略。

### 排查结论
- OTP 验证码邮件路径 `/api/send-otp` 确实调用 `sendBrevoEmail`（Brevo），magic link `/api/send-magic-link` 也是 Brevo，注册 `/api/sign-up` 用 `email_confirm:true` 不发邮件。
- 全量 grep `src/` + `app/`：**无任何** `supabase.auth.signInWithOtp` / `signUp` 调用，应用从不走 Supabase 自带邮件器。
- 真正 bug：`check_otp_rate_limit(p_email)` 函数语义是 `not exists(60秒内记录)` → 返回 `true` 表示「可以发」。
  但路由写成 `if (limited) return 429`，把「可以发」误判为限频，**首条请求就被 429 拦截，Brevo 永远发不出**。用户因此收不到 Brevo 邮件，误以为是 Supabase 自带。

### 修复
- `app/api/send-otp/route.js`：改为 `allowed` 语义 + `if (!allowed) return 429`，并用 try/catch 包裹 RPC（函数缺失时放行，避免硬 500）。
- 不改动 SQL 函数（`not exists` = allowed 与新路由语义一致），无需跑迁移，Vercel 部署即生效。

### 提醒用户
- 确认 Vercel 环境变量 `BREVO_API_KEY` / `BREVO_SENDER_EMAIL` / `BREVO_SENDER_NAME` 已配置（缺失时 `sendBrevoEmail` 抛错 → 502，非 429）。当前 429 与此无关。

### 构建与推送
- `npm run build` 通过 → 提交并 `git push origin main` → Vercel 自动部署。

## 修复 /api/send-otp 500：otp_codes 缺少 purpose 列

### 现象
- 用户点击「发送验证码」提示：
  `发送失败：Could not find the 'purpose' column of 'otp_codes' in the schema cache`
- F12：POST /api/send-otp 500 (Internal Server Error)

### 排查结论
- `/api/send-otp` 和 `/api/verify-otp` 都向 `otp_codes` 写入/查询 `purpose` 字段。
- 当前生产库 `otp_codes` 表没有 `purpose` 列；01-create-schema.sql 已有该列，但旧库未同步。

### 修复
- 新增 `supabase/migrations/06-add-purpose-to-otp-codes.sql`：
  - `alter table otp_codes add column if not exists purpose text not null default 'login';`
  - `create index if not exists idx_otp_email_purpose on otp_codes (email, purpose);`
  - 把旧记录 `purpose` 统一为 `'login'`。

### 用户下一步
- 在 Supabase SQL Editor 中执行 `supabase/migrations/06-add-purpose-to-otp-codes.sql` 后，/api/send-otp 即可正常写入并通过 Brevo 发信。

## 修复 /api/send-otp 500：otp_codes 缺少 type 列（not-null 约束）

### 现象
- 执行 06 后仍报错：`"otp_codes" violates not-null constraint` / `null value in column "type" of relation "otp_codes"`。
- 说明现有库的 otp_codes 表除了缺 purpose，还有一个 NOT NULL 的 `type` 列，而路由只写入了 email/code/purpose/expires_at。

### 修复
- 新增 `supabase/migrations/07-otp-codes-type-column.sql`：
  - `add column if not exists purpose text not null default 'login';`（幂等，兼容 06 未跑）
  - `add column if not exists type text not null default 'email';`
  - 回填旧记录，建 `(email, purpose)` 索引。
- `app/api/send-otp/route.js` 的 insert 增加 `type: 'email'`。

### 用户下一步
- 在 Supabase SQL Editor 执行 `supabase/migrations/07-otp-codes-type-column.sql`（06 已跑过也无妨，脚本幂等）。
- 若再报其他列的 not-null，按同样模式补列 + 在 insert 中给默认值即可。

## 修复 /api/send-otp 500：otp_codes.type 的遗留 CHECK 约束

### 现象
- 执行 07 后仍报错：`new row for relation "otp_codes" violates check constraint "otp_codes_type_check"`。
- 现有库 `otp_codes.type` 列带有一个 CHECK 约束，其允许值集合与路由写入的 `'email'` 不符（约束是旧 schema 遗留）。

### 修复
- 新增 `supabase/migrations/08-drop-otp-type-check.sql`：
  - `DO $$ ... alter table otp_codes drop constraint if exists otp_codes_type_check;`（幂等）。
- 路由已写入 `type: 'email'`，移除约束后即可正常插入。

### 用户下一步
- 在 Supabase SQL Editor 执行 `supabase/migrations/08-drop-otp-type-check.sql`。
- 预期：/api/send-otp 不再 500，验证码经 Brevo 发出，可完成登录。

## /api/send-otp 502（DB 写入已通过，卡在 Brevo 发信）

### 现象
- 执行 08 后不再 500，改为 `POST /api/send-otp 502`。
- 路由在 Brevo 调用失败时 `return 502`，说明 otp_codes 已写入成功，问题出在邮件发送环节。

### 已做
- `app/api/send-otp/route.js` 的 catch 增加 `console.error('[send-otp] Brevo 发送失败:', e)`，便于在 Vercel Function 日志看到真实错误。

### 可能原因（需用户确认）
1. **Vercel 未配置 BREVO_* 环境变量**：本地 `.env.local` 被 gitignore，不会自动部署。需在 Vercel Dashboard → Settings → Environment Variables 手动加 `BREVO_API_KEY` / `BREVO_SENDER_EMAIL` / `BREVO_SENDER_NAME`（或 `vercel env import .env.local`）。
2. **Brevo 发件人邮箱未验证**：Brevo 要求 sender 邮箱/域名在后台 Senders 中激活（绿勾），否则返回 400。
3. **API Key 无效/权限不足**：Brevo 返回 401。

### 用户下一步
- 把页面上「发送失败：」后面的具体文字发我，或看 Vercel 日志 `[send-otp] Brevo 发送失败:` 的内容。
- 确认 Vercel 环境变量与 Brevo 发件人验证状态。

## /api/send-otp 502 具体错误：Brevo 返回 HTML 而非 JSON

### 现象（用户贴出的页面文字）
- `发送失败： Unexpected token '<', "<!DOCTYPE "... is not valid JSON`
- 这是 `brevo.js` 原代码在 `res.ok` 时直接 `res.json()` 解析到 HTML 页面抛出的错。

### 根因推测
- Brevo 接口返回了 HTML 页（而非 JSON），常见原因：
  1. 请求被重定向到登录/网关页（如 Vercel 出站被拦截、或 API Key 触发代理）；
  2. 端点返回 404/错误 HTML（路径或区域不对）；
  3. `BREVO_API_KEY` 值带多余引号/空格导致鉴权失败被跳转到 HTML 登录页。

### 已做
- `src/lib/brevo.js` 改为：先 `res.text()` 取原始响应，判断是否为 HTML；
  - 非 2xx：抛出 `Brevo HTTP {status}: {原始片段}`；
  - 2xx 但却是 HTML：抛出 `Brevo 返回了非 JSON（疑似网关/代理页）: {片段}`；
  - 否则 `JSON.parse(raw)`。
- 这样部署后页面会显示**具体 HTTP 状态 + HTML 前 200 字符**，便于定位。

### 用户下一步
- 重新触发一次发送，把新的「发送失败：」文字发我（应包含 HTTP 状态与 HTML 片段）。
- 同时核对 Vercel 里 `BREVO_API_KEY` 值没有多余引号/换行，且 Brevo 后台 Senders 中发件人已验证。

## 进一步加固：路由全局 try/catch + 前端非 JSON 降级

### 现象
- 用户再次截图仍是 `Unexpected token '<'...`。
- 这意味着服务端在某种情况下仍返回 HTML（可能是未捕获异常直接抛出，也可能是 Vercel 网关/代理页）。原路由已逐个 catch 但缺少最外层兜底，前端 `res.json()` 遇 HTML 即崩。

### 修复
- `app/api/send-otp/route.js`：整体 `POST` 处理器外套一层 try/catch，任何未捕获异常都返回 JSON 500，杜绝 HTML 漏出。
- `src/lib/db/auth.js` 的 `sendOtp`：解析 JSON 失败时改读 `res.text()`，返回带 HTTP 状态码和原始响应片段的错误信息。

### 用户下一步
- 硬刷新后重新发送，页面会显示类似 `服务端返回了非 JSON 响应（404/500/502）：...` 的具体内容。
- 把这段内容发我，就能定位是 Brevo 返回 HTML、Vercel 网关拦截、还是路由异常。

## /api/send-otp 仍空 502 → 改为并行 + maxDuration，彻底消除空 502

### 根因确认
- 空 502 = Vercel Function 在平台层被超时掐断（路由内 try/catch 来不及返回 JSON）。
- 旧路由：限频 RPC(5s) + 写库(5s) + Brevo(10s) **串行**，最坏 ~20s 远超 Hobby 10s 上限 → 网关/代理丢空 502。
- 用户已重传 BREVO_API_KEY / 发件人，故不是缺环境变量，而是网络调用叠加超时。

### 修复（重写 route）
- `export const maxDuration = 10`（显式声明 Hobby 上限）。
- 限频检查 `check_otp_rate_limit` 改为**后台触发、不 await**，不阻塞主流程。
- 写库 + Brevo 发信改为 `Promise.all` **并行**，总耗时 = max(写库, 发信) ≈ 8s，< 10s。
- insert 失败 Supabase 返回 `{error}` 不 reject，故单独 `insRes.error` 检查；Brevo 用 `.then(()=>null).catch(e=>e)` 捕获错误对象，避免 Promise.all 误判成功。
- 起始 `console.log('[send-otp] env:', {hasBrevoKey, hasSender, hasSupabaseUrl, hasServiceKey})` 确认变量注入情况（不打印值）。
- 超时/异常均返回带文字的 JSON 502，不再有空 502。

### 部署后预期
- 若 Brevo 从 Vercel 区域可达：正常收到邮件。
- 若 Brevo 不可达：页面显示 `发送失败：Brevo 发送超时（8000ms 未完成）`，可据此确认是 Brevo 网络问题 → 需换 SMTP/代理。
- 用户应在 Vercel Dashboard → Logs 查 `[send-otp] env` 确认四个变量均为 true。

## /api/send-otp 502 根因最终定位 = Brevo 字段名错（非超时、非网络）

### 用户提供的 Vercel 日志关键证据
- 函数 2s 即结束，3 个外部请求均发出（Brevo / 限频 RPC / otp_codes），非超时。
- `[send-otp] env: { hasBrevoKey:true, hasSender:true, hasSupabaseUrl:true, hasServiceKey:true }` —— 四个变量均注入。
- `[send-otp] Brevo 失败: Error: Brevo HTTP 400: {"code":"missing_parameter","message":"Either of htmlContent or textContent is required"}`

### 真因与修复
- `src/lib/brevo.js` 请求体用了 `html` / `text`，但 **Brevo v3 要求 `htmlContent` / `textContent`** → 400 missing_parameter。
- 改 `brevo.js` 请求体：`html → htmlContent`、`text → textContent`（函数签名不变，调用方 send-otp / send-magic-link / send-reminder 三处统一受益）。
- 提交 `fix: Brevo 请求字段名 html/text 改为 htmlContent/textContent` → push。
- 注：此前「错误改 200 响应 + 前端按 ok 解析」已让代理不再吞正文，故本次能直接看到 Brevo 真实报错，定位极快。

### 经验沉淀（可复用）
- **Vercel + 代理域名（如 182183.xyz）会吞掉 502 响应体**，导致前端看到空 502；诊断时务必走 Vercel Dashboard Logs 看函数真实输出。
- **任何对外 API 接入都要先核对官方字段名**（Brevo 是 htmlContent/textContent，不是 html/text）。
- 代理吞正文时，把错误响应统一改成 `200 + {ok:false, error}` 是通用解法。

## verify-otp 报 "Email logins are disabled" → 绕过 signInWithPassword

### 根因
- 后端 `verify-otp` 已成功自验 OTP + admin 创建用户，但返回**临时密码**给前端；前端 `src/lib/db/auth.js:164` 用 `supabase.auth.signInWithPassword` 换 session 时，Supabase 项目 **Auth → Providers → Email 开关关闭** → 直接报 `Email logins are disabled`。
- `sign-up` 路由同样用 `signInWithPassword` 自动登录，会踩同一坑。
- magic-link 路由走 admin `generateLink`（不受该开关限制），本身无此问题。

### 修复（代码层绕过，无需改 Supabase 后台）
1. 新增 `src/lib/supabaseServer.js# mintSessionByEmail(supabase, email)`：
   - `supabase.auth.admin.generateLink({ type:'magiclink', email })` 取 `properties.hashed_token`；
   - 直接 `POST {URL}/auth/v1/verify`（apikey+Bearer=anon key，body `{token, type:'magiclink'}`）兑换 `{access_token, refresh_token}`。
   - 该版本 supabase-js（2.110.2）**无 `verifyToken` 方法**，故用手写 REST 复刻（node 实测 `c.auth.verifyToken === undefined`）。
   - generateLink 与 verify 端点均不受 Email provider 开关限制，故可在 Email 关闭下完成登录。
2. `verify-otp` 路由：删临时密码逻辑，改 `mintSessionByEmail` 后返回 `{ data:{ email, user_id, session:{access_token,refresh_token} } }`。
3. `auth.js# verifyBrevoOtp`：用 `supabase.auth.setSession({access_token, refresh_token})` 落地（不再 signInWithPassword）。
4. `sign-up` 路由：创建用户后同样 `mintSessionByEmail` 返回 session；`auth.js# signUpWithEmail` 优先 `setSession`，失败回退 `signInWithPassword`。
- 提交 `fix: 绕过 signInWithPassword，verify-otp/sign-up 改用服务端签发会话` → push。

### 风险与兜底
- 若 Supabase 的 verify 端点对 magiclink 也校验 Email provider 开关（存疑），部署后仍可能报 "Email logins are disabled"；届时唯一可靠修复是 **Supabase Dashboard → Authentication → Providers → Email 开启**（任何邮箱登录方式的硬前提）。已告知用户。

## verify-otp 报 "Only an email address or phone number should be provided on verify" → 补 email 字段

### 现象
- 用户部署后仍报：`验证失败：签发会话失败：Only an email address or phone number should be provided on verify`；`POST /api/verify-otp 500`。
- 这是 Supabase GoTrue `/auth/v1/verify` 在 body 缺 `email`/`phone` 时抛的统一错误。

### 根因
- `mintSessionByEmail` 调 verify 时 body 只发了 `{ token, type:'magiclink' }`，未带 `email`，GoTrue 无法定位用户 → 500。

### 修复
- `src/lib/supabaseServer.js`：verify 请求体改为 `{ token: hashedToken, type:'magiclink', email }`。
- `npm run build` 通过 → 与代码同一次 git push（不单独推 memory，避免 Vercel 二次部署）。

### 用户偏好提醒（已执行）

## send-otp 04:38 旧部署报 missing_parameter → 04:58 已修复但邮件收不到

### 现象
- 用户贴两段 Vercel log：04:38 Brevo 失败 HTTP 400 missing_parameter (htmlContent/textContent)（旧部署 html/text bug 残留）；04:58 写库 + Brevo 均完成（htmlContent 修复已上线，Brevo 不再抛错）。
- 但用户反馈现在直接收不到验证码，连 magiclink 也收不到，尽管路由显示 Brevo 成功。

### 结论
- 代码路径正确：send-otp / send-magic-link 共用 src/lib/brevo.js（已用 htmlContent/textContent），Brevo API 返回成功。非代码 bug，是 Brevo 投递/账号侧问题。代码自能收到验证码那版起未变（仅 supabaseServer.js 改过），回归点在 Brevo 账号/环境变量。

### 已做（诊断增强，非修复）
- app/api/send-otp/route.js：Brevo 结果改为捕获 ok/res/err，成功时 console.log messageId。
- app/api/send-magic-link/route.js：成功时同样 log messageId。
- 构建通过，与 memory 同一次 push。

### 待用户排查（Brevo 后台，非代码）
1. 发件人是否验证：Brevo Settings Senders & IP，BREVO_SENDER_EMAIL 是否绿勾。
2. 域名 SPF/DKIM：发件域名需在 Brevo 配置并 DNS 生效，否则 Gmail 判垃圾/丢弃。
3. 账号是否激活：Brevo 免费版需手机激活，未激活只能发几条测试信之后静默拦截（与之前能收到、现在收不到高度吻合）。
4. 垃圾箱：Gmail 拦截后通常在垃圾邮件。
5. 重新触发后看 Vercel log 是否出现 messageId=...：有则确认 Brevo 接受、问题在投递；无/报错则把新 log 发我。

## 决定性证据：Brevo 已发出（messageId 存在），邮件卡在 Gmail 投递

### 用户提供的日志（05:04 / 05:05）
- `[send-otp] Brevo 已接受 messageId= <202607200504.59466819962@smtp-relay.mailin.fr>`
- `[send-magic-link] Brevo 已接受 messageId= <202607200505.74119576545@smtp-relay.mailin.fr>`
- 两个都是 Brevo 真实 SMTP 中继 ID（smtp-relay.mailin.fr），证明邮件已被 Brevo 中继发出。

### 结论
- 代码侧 100% 正常：Brevo 接收并中继了邮件。问题在 Brevo 中继 → Gmail 这一段，被 Gmail 拒收或进垃圾箱。
- 用户称 Brevo 账号/发件人已验证。但「发件人验证」≠「域名 SPF/DKIM 认证」。
- 最可能根因：BREVO_SENDER_EMAIL 的域名未在 Brevo 配置 SPF/DKIM（DNS），或发件人用了 @gmail.com 等第三方域名（Gmail 因 SPF/DKIM/DMARC 不对齐直接拒收/判垃圾）。

### 待用户执行（Brevo 后台，真修复）
1. Brevo → Logs 查上述 messageId 的状态（Sent/Delivered/Blocked/Bounced）；若 Delivered 但 Gmail 无信 → 必在垃圾箱或域名未认证。
2. 若 BREVO_SENDER_EMAIL 是 @gmail.com/@qq.com 等第三方域：**不能用**（Gmail 拒收）。必须改成自己拥有的域名（如 @thaidict.182183.xyz）。
3. Brevo → Settings → Senders & IP → 选中该域名 → 按提示在 DNS 加 SPF + DKIM 两条 TXT 记录并等待生效（域名认证是 Gmail 收信硬前提）。
4. 查 Gmail 垃圾邮件箱。

### 本回合无代码改动，故未单独 push（遵守用户「memory 不与代码同推会白耗 Vercel 部署」的要求）；待下次代码改动一并推送。

## 对照分析 gongwen-os-v2：为何它一直能收邮件/magiclink 且能登录

### 架构差异（已克隆 /tmp/gongwen-os-v2 读源码）
- gongwen-os-v2 **不用 Supabase Auth**：DB 用 libSQL(Turso)+Drizzle；登录用自签 JWT（jose）+ bcryptjs；会话存 httpOnly cookie。
- 邮件走 **Brevo REST API**（`src/server/email/send.ts`），发件人 `FROM = noreply@gongwenos.182183.xyz`（代码默认，可被 EMAIL_FROM 覆盖）。
- 流程：`send-code` 生成 6 位验证码 + 48 位 magic token，存自己 `verification_tokens` 表（10min 过期），一封邮件同时含验证码和 magic-link(`/auth/magic-link?token=`)；`magic-login`/`verify-code` 验 token 后自签 JWT。完全是自家闭环，不碰 Supabase Email 开关，故从不会 "Email logins are disabled"。

### 关键差异（决定性）
- gongwen-os-v2 发件域 `gongwenos.182183.xyz` 在 Brevo 已做 SPF/DKIM 认证 → Gmail 信任 → 一直能收。
- thaidict 发件域 `thaidict.182183.xyz`（取自 .env.local `BREVO_SENDER_EMAIL=noreply@thaidict.182183.xyz`）**未在 Brevo 认证** → Gmail 因 SPF/DKIM/DMARC 不对齐拒收/判垃圾 → 收不到（尽管 Brevo 已中继，有 messageId 为证）。
- 两者同属 182183.xyz 域、同一 Brevo 账号。gongwenos 子域已认证，thaidict 子域未认证。

### thaidict 收不到邮件的真修复（配置级，非代码）
- 方案 A（秒修、零 DNS）：Vercel 把 `BREVO_SENDER_EMAIL` 改成 `noreply@gongwenos.182183.xyz`（复用已认证发件人），重发即达。
- 方案 B（正规）：Brevo → Settings → Senders & IP → 添加并认证 `thaidict.182183.xyz`（加 SPF + DKIM 两条 TXT 到该子域 DNS），保留 `noreply@thaidict.182183.xyz`。
- 代码无需改动（thaidict 的 send-otp/send-magic-link/brevo.js 都已正确，仅差发件域认证）。
- 用户要求：memory 不要每次分开推送，应跟项目改动一起推，免得 Vercel 部署两次浪费额度。后续一律单次 `git add . + commit + push`。

> ⚠️ 勘误：上面「thaidict.182183.xyz 未在 Brevo 认证」是**错误判断**。用户随后明确说明：`thaidict.182183.xyz` **已经认证过 SPF/DKIM**，且他手动重新复制过 Brevo 的 API Key/发件人（替换成最新）。所以「收不到邮件」并非域名未认证，而是早前 Brevo 配置/旧部署残留；认证本身没问题。

## 邮箱登录按 gongwen-os-v2 重做（自建 verification_tokens + 自建魔法链接）

### 用户指令
- 「邮箱方面你就按照公文系统重做吧，google、github登录目前正常的不要动了。」
- 即：邮箱验证码/magic link 整套改成 gongwen 风格（自建验证码+魔法令牌同表、Brevo 一封邮件同时带验证码和自建魔法链接、魔法链接走自己的端点）；Google/GitHub（Supabase OAuth）保持不变。

### 设计决策
- **会话模型保留 Supabase Auth**（不动 Google/GitHub，且 AppContext/前端只读 `supabase.auth`）。邮箱用户仍签发 Supabase 会话（用 `mintSessionByEmail` = admin `generateLink(magiclink)` + 直接 POST `/auth/v1/verify`，已绕过 Email 开关）。
- 仅把「邮箱验证」这一步从 Supabase OTP / Supabase 托管 magic link 改为**自建**，彻底绕开 Email 开关与 magic-link 网关。

### 改动清单
1. `supabase/migrations/09-verification-tokens.sql`（新增）：建 `verification_tokens(id,email,code,token,purpose,expires_at,used_at,created_at)` + 索引 + 开启 RLS（仅 service_role 可访问）；`check_otp_rate_limit` 改读 `verification_tokens`；`cleanup_expired_otps` 同时清两张表。
2. `app/api/send-otp/route.js`（重写）：生成 6 位 code + 48 位 `crypto.randomBytes(24).hex` token，写 `verification_tokens`（先删该邮箱未用记录），**一封 Brevo 邮件同时含验证码与 `${NEXT_PUBLIC_SITE_URL}/?magic_token=` 链接**。

## 部署约束：迁移需手动执行（沙箱无法代跑 DDL）
- 2026-07-20 实测：沙箱能连 Supabase（`curl https://zvemahqskgluhirzbcqu.supabase.co/rest/v1/` 返回 401＝网络通），但**无法自动执行 migration DDL**——`supabase` CLI 未登录（`~/.supabase` 不存在）、`.env.local` 无数据库直连密码 / Personal Access Token。
- 故 `09-verification-tokens.sql` 必须由用户执行：① Supabase SQL Editor 粘贴 Run；或 ② 本地 `supabase db execute --project-ref zvemahqskgluhirzbcqu --file supabase/migrations/09-verification-tokens.sql`（需 CLI 登录）；或 ③ 提供 Personal Access Token 由 AI 代跑。
- 该迁移纯 `if not exists` / `create or replace`，幂等安全，不碰 dictionary 6 万词数据。

## Bug 修复：verify-otp / magic-login 报「签发会话失败：Token has expired or is invalid」
- 现象：09 表建好后登录仍报 403 otp_expired。
- 根因：`supabaseServer.js#mintSessionByEmail` 手写 `POST /auth/v1/verify` 的 body 错用 `token`（应为 `token_hash`），且**多带了 `email`**。GoTrue 对 magiclink verify 要求 body 仅 `{ token_hash, type:'magiclink' }`；带 email 报 "Only the token_hash and type should be provided"，字段名错则报 "Token has expired or is invalid"。
- 修复：body 改为 `{ token_hash: hashedToken, type:'magiclink' }`（去掉 email）。`hashed_token` 提取路径 `linkData.properties.hashed_token` 正确；verify 返回 `access_token/refresh_token` 为平铺字段，原读取方式无误。
- 验证：沙箱用 .env.local 的 service_role key 实测 `generateLink`+`verifyOtp({token_hash,type:'magiclink'})`（不带 email）→ 200 且 hasSession=true；直接 fetch 改后 body → 200 且平铺 access_token 存在。`npm run build` 通过。
3. `app/api/verify-otp/route.js`（重写）：按 code 查 `verification_tokens`（最新、未用、未过期）→ 标记 used_at → `ensureUserAndMintSession` 建/确认用户并签发会话。
4. `app/api/magic-login/route.js`（新增）：按 token 查 `verification_tokens` → 标记 used_at → 同样签发会话。
5. `src/lib/supabaseServer.js`：新增 `ensureUserAndMintSession(supabase, email)`（admin listUsers/createUser + email_confirm + mintSessionByEmail），供 verify-otp / magic-login 复用。
6. `src/lib/db/auth.js`：`sendMagicLink` 改为复用 `sendOtp`（同一封组合邮件）；新增 `verifyMagicToken(token)`（调 /api/magic-login → setSession）。
7. `src/context/AppContext.jsx`：`handleAuthCallback` 新增识别 `?magic_token=` → 调 `verifyMagicToken` 完成登录（与 OAuth 的 `?code=` / `#access_token` 分支并列）。
8. `src/screens/LoginPage.jsx`：链接 tab 文案改为「登录邮件已发送（含验证码与一键登录链接）」。
9. 删除已废弃的 `app/api/send-magic-link/route.js`（Supabase 托管 magic link，已被自建替代；`sendMagicLink` 改调 send-otp）。

### 部署前置（用户需在 Supabase 跑迁移）
- 生产库必须执行 `supabase/migrations/09-verification-tokens.sql` 才能建表，否则 send-otp 写库报 relation 不存在。
- Vercel 环境变量需有 `NEXT_PUBLIC_SITE_URL`（魔法链接用，记忆中此前已配）。

### 构建与推送
- `npm run build` 通过（/api/magic-login、/api/send-otp、/api/verify-otp 均注册）。
- 与 memory 同一次 `git add -A + commit + push origin main`（用户要求不分推）。

## 统一邮件模板（奶油/琥珀泰式纹样 + favicon logo）

### 用户指令
- 所有邮件模板（验证码、学习提醒、未来任何发信事项）统一改用给定 HTML 样式；左上角 logo 用项目主 favicon（原 CSS 画的金色菱形圆点替换成真实 favicon）。

### 实现
- 新增 `src/lib/emailTemplate.js`：`renderEmail({ heading, introHtml, code, codeLabel, primaryAction, bodyHtml, bottomTips })` → 返回 `{ html, text }`（自带 `<style>` 与 `<!DOCTYPE>`，纯内联、无需外部 CSS）。样式完全照搬用户给的模板（#faf5eb 底、#fffdf8 卡、#a67d47/#704f2b 琥珀棕、泰式纹样边框、`.code-wrap` 暗纹、`.magic-link` 渐变按钮）。
- logo：`.logo-mark` 由 CSS 圆点改为 `<img class="logo-mark" src="${SITE_URL}/icons/icon-email.png">`（圆形裁切 + 细金边）。**邮件客户端（尤其 Gmail）拦截 SVG 与 data: URI 图片**，故 favicon 必须是 PNG 且用站点绝对地址，不能嵌 SVG。
- 新增 `public/icons/icon-email.png`：`sips` 把 `icon-192.svg`（layout.jsx 引用的主 favicon）转 128×128 PNG（2.4KB）。
- `app/api/send-otp/route.js`：Brevo `html/text` 改由 `renderEmail` 生成（heading「中泰智能学习者词典登录验证」+ 验证码块 + Magic Link 按钮 + 单次可用/10分钟有效提示；验证码有效期按真实 10 分钟，文案写「10 分钟有效」）。
- `app/api/send-reminder/route.js`：同样改 `renderEmail`（heading「今日学习提醒」+ 任务列表 + 「👉 打开词笺」按钮），视觉与验证码邮件一致。
- 未来新增发信事项：统一 `import { renderEmail }` + `sendBrevoEmail({ to, subject, ...renderEmail({...}) })`，保持一致风格。

### 构建与推送
- `npm run build` 通过。代码 + 模板 + 新 PNG + memory 同一次 `git add -A + commit + push origin main`。

## 邮件模板改简约风 + 字体 + 审阅提醒邮件

### 用户指令
- 上一版（泰式纹样 + favicon logo）太丑，统一改回更简约的模板（#f8f5ee 底、#fffdf8 卡、#7c5c36/#916c3c 琥珀棕、无纹样、border-radius 4px、.magic-link radius 2px）。
- **字体**：中英文用 Noto Serif SC（思源宋体简体），泰文用 Sriracha；通过 Google Fonts `@import` 引入，正文 font-family `"Noto Serif SC","Sriracha",serif`（按字形回退，泰文自动落到 Sriracha），另加 `.thai` 类可强制泰文 Sriracha。
- **新增「审阅提醒」邮件**：用户提交（AI 生成）新单词进入待审批后，所有「有审阅权限的管理员」都要收到通知邮件。

### 实现
- 重写 `src/lib/emailTemplate.js`：新简约 CSS（去掉 `.box::before` 纹样、去掉 `.logo`/favicon logo 块），新增 `@import` 字体 + `.thai` 类；`renderEmail` 签名不变（send-otp/send-reminder 自动套用新风格）。
- 新增 `app/api/send-review-reminder/route.js`：POST {type,payload,requestedBy} → service_role 查 `user_roles`（role=super_admin，或 admin 且 permissions 含 `approve_entries`）→ `supabase.auth.admin.listUsers()` 取邮箱 → 用 `renderEmail` 给每位审阅者发「新内容待审阅」邮件（含泰文词条/拼音/义项，.thai 渲染泰文）。统一返回 200+{ok}（防代理吞）。
- `src/lib/db/approvals.js`：新增 `notifyReviewers({type,payload,requestedBy})`（fire-and-forget `fetch('/api/send-review-reminder', {keepalive:true})`）。
- `src/context/AppContext.jsx`：`handleGenerated` 在 `createPendingApproval` 成功后调用 `notifyReviewers`（已 import）。
- 审阅者判定逻辑复用 `roles.js` 的权限语义（`approve_entries`）。

### 构建与推送
- `npm run build` 通过（/api/send-review-reminder 已注册）。代码+模板+新路由+memory 同一次 `git add -A + commit + push origin main`（不双推）。
- ⚠️ 审阅邮件依赖 `user_roles` 表中真实的管理员记录（role/permissions）与 `auth.users` 邮箱；若库里没配管理员或邮箱缺失，则 `sent=0`（路由已容错，不报错）。

## 登录页与邮件模板微调（用户反馈）

### 用户指令
- 登录页主 logo 再小一些（190→150）。
- 点「验证码/链接登录」后**不要再出现**「邮箱验证码」「邮箱链接」两个小按钮（本就相结合）。
- 「密码登录」/「验证码/链接登录」两个 Tab 不够明显：点选后应以**完整圆角矩形白色背景**显示，且这个矩形要比外围容器（Card）小一圈（内缩分段控件）。
- 邮件模板字体偏大：标题/引导语/按钮/数字框字号都缩小一点，但**不能小于分割线下的文字**（bottom-tips 13px 为下限）。

### 实现
- `src/screens/LoginPage.jsx`：`MainLogo size={190}`→`{150}`；删除 `otpKind` state 与 `onMagicLink`/`sendMagicLink` import（已无用）；OTP 分支简化为单流程（邮箱输入 → 发「验证码/链接」按钮 → 验证码输入+验证，底部加「邮件已同时包含验证码与一键登录链接」提示）。
- Tab 切换改为**分段控件**：外层 `var(--c-p100)` 轨道 + `padding:6 + gap:6`，选中态 `var(--c-surface)` 白底 + `border-radius:10 + boxShadow`，白块相对 Card 内缩一圈，明显区分。
- `src/lib/emailTemplate.js` 字号下调：h2 24→20、.content 15→14、.auth-code 36→30（letter-spacing 10→8）、.magic-link 16→14、.word-title 26→22、codeLabel 显式 14px；.bottom-tips 维持 13px（下限，不动）。

### 构建与推送
- `npm run build` 通过（无 otpKind/sendMagicLink 残留引用）。代码+模板+memory 同一次 `git add -A + commit + push origin main`（不双推）。

## Bug：AI 生成词条详情崩溃（i.join is not a function）

### 现象
- AI 生成词条进入「词条详情」页报错 `TypeError: i.join is not a function`（app/page 主包 chunk）。

### 根因
- `src/screens/WordDetailPage.jsx` 的 `RelWord` 组件对近/反义词、学习者联想词做 `meaning.join('；')`。
- 普通词典词：`s.meaning` 取 `getWordMeanings` 返回**数组**；AI 生成词：相关词直接带 `meaning` **字符串**（如「朋友」），字符串无 `.join` → 崩溃。

### 修复
- `RelWord` 内将 `meaning` 规整为数组：`Array.isArray(meaning) ? meaning : (meaning ? [meaning] : [])`，再判 `length>0` 后 `join`。字符串/数组/空均安全。

## Bug：手动生成词条近义词显示为 JSON 字符串

### 现象
- 词条详情页「近义词」区块显示原始 JSON 文本，如 `{"zh":"地址和姓名","word":"ที่อยู่ และ ชื่อ "}`，而不是可点击的泰语词。

### 根因
- `synonyms` 数组元素可能以 JSON 字符串形式存储（`"{\"zh\":\"...\",\"word\":\"...\"}"`），`src/lib/utils.js` 的 `normalizeRel` 把它当作普通字符串处理，直接塞进 `word` 字段渲染。

### 修复
- `src/lib/utils.js`：`normalizeRel` 对字符串先尝试 `JSON.parse`；若解析出对象且含 `word`，取 `word` 和 `zh/meaning`。
- `src/screens/WordDetailPage.jsx`：`RelWord` 对 `word` prop 做类型防御（字符串/对象均可），兜底 `String(word)`。

### 构建与推送
- `npm run build` 通过。代码+memory 同一次 `git add -A + commit + push origin main`（不双推）。

## Bug：审批后社区词记录写入失败（no unique constraint matching ON CONFLICT）

### 用户反馈
- 管理员批准词条后提示「已批准入库主词典，但社区词记录写入失败：there is no unique or exclusion constraint matching the ON CONFLICT specification」。用户确认主词典已能搜到新词。

### 概念澄清
- **社区记录** = `community_words` 表里的「社区贡献词条」记录（含 source/submitted_by/zh_hint）。它与**主词典 `dictionary`** 是两套独立存储：审批通过后 `dictionary` 写入 → 汇入 `dictionary_full` 视图 → 全库可搜；同时代码还往 `community_words` 写一条贡献记录。

### 根因
- `src/lib/db/community.js` 的 `saveCommunityWord` 真实库分支用 `upsert(row, { onConflict: 'word' })`，依赖 `word` 列唯一约束。
- 用户的库是**已有库**（跑 `02-sync-existing.sql` 对齐旧 31 张表），旧系统建 `community_words` 时**未对 word 建唯一约束** → upsert 找不到冲突约束报错。主词典写入不受影响，所以仍能搜到。

### 修复
- `src/lib/db/community.js`：真实库分支改为「先 `select` 查 word 是否已存在 → 存在则 `update`、不存在则 `insert`」，不再依赖唯一约束。
- 新增 `supabase/migrations/10-add-community-words-unique.sql`（可选增强）：去重后给 `community_words(lower(word))` 建唯一索引，与 dictionary 对齐、防止社区词重复（dictionary_full_ext 视图 UNION ALL 会因此重复）。代码已不依赖它。

### 构建与推送
- `npm run build` 通过。代码+memory 同一次 `git add -A + commit + push origin main`（不双推）。

## 详情页交互改造：加复制气泡 + 删底部按钮

### 用户指令
- 词条/句子/短语详情页：主词卡片右侧喇叭按钮下加一个**复制按钮**；因主词被分词，点击复制按钮弹出仅含**纯文本主词**的小气泡，用户自行选中文字复制（不做自动复制）。
- 词条（含句子短语）详情最底部三个按钮「加入文件夹、词形分析、笔记」全部删除。

### 实现
- 新建 `src/components/CopyPopover.jsx`：复制图标按钮 + 点击出现的纯文本气泡（position:absolute 右下对齐，点击外部/Esc 关闭，文字 `userSelect:text` 可选中）。props: `text`/`title`/`buttonStyle`。
- `src/screens/WordDetailPage.jsx`：右侧按钮组改竖向 column（朗读 + CopyPopover `text=data.word`）；删底部三按钮 JSX；删 `showMorph`/`showNote` state 及其子页面渲染、删 `MorphologySection`/`NoteEditorSection` import、删 `actionBtn` 常量。收藏入口（顶栏星标 → openFolder 加单词夹）保留。
- `src/components/SentenceDetailView.jsx`（句子/短语详情复用）：右侧改竖向 column（朗读 + CopyPopover `text=sentence.thai` + 收藏）；删底部「加入文件夹」按钮及其 `actionBtn` 常量。收藏（星标 → openFolder 加句子夹）保留。

### 构建与推送
- `npm run build` 通过。代码+memory 同一次 `git add -A + commit + push origin main`（不双推）。


## 逐词分析加中文释义 + 搜索结果按词频排序

### 用户指令
1. 句子/短语的逐词分析处，每个划线泰文 token 后括号显示中文释义，参考近反义词的括号实时查询功能。
2. 搜索词语时按词频顺序排，频率越高越靠前；词频表中没有则按搜索关联度排，优先义项匹配，然后例句、近反义词匹配。

### 实现
- `src/components/ThaiSentence.jsx`：新增 `meaningMap` state；在 `showMeanings` 模式下对没有自带 `meaning` 的分词 token 并发调用 `lookup`（默认 `getWordMeanings`）预查中文释义；渲染时优先用 token 自带 meaning，否则用 `meaningMap` 前 3 条 `join('；')` 在括号内显示。WordBubble 点击查词保持独立。
- `src/lib/db/search.js`：新增 `scoreSearchResult`/`sortSearchResults` 辅助函数；评分规则：先按 `freq_ttc ?? freq_tnc ?? freq_phupha` 降序（无词频视为 `-Infinity` 放最后），词频相同则按匹配类型排序：精确词匹配 > 词包含 > 义项匹配 > 例句匹配 > 近反义词匹配。`mockSearch` 与 `searchWords` 真实库分支均应用该排序；mock 的中文/例句/近反义词匹配范围也同步扩大。

### 构建与推送
- `npm run build` 通过。代码+memory 同一次 `git add -A + commit + push origin main`（不双推）。



## 词频表扫描 + 排序 bug 修复

### 用户疑问
- 词频表在 `word_freqs` 表中，前端查不到这个表吗？要求用 CLI 扫真实库确认。

### CLI 扫描结论（前端同款 anon key 直连 Supabase）
- `word_freqs` 表 **anon 可查（200）**，RLS 允许读；无需让前端改查该表。
- 表结构 `{word, corpus, frequency}`：`corpus` 分 `tnc`/`ttc`/`phupha`（每词每语料库一行）。
- 代码查的 `dictionary_full_ext` 视图**已映射词频**到 `freq_tnc`/`freq_ttc`/`freq_phupha` 三列（例 `ที่` = 818364/63126/162917982，全部有值）。视图即是词频的聚合出口，前端无需直连 `word_freqs`。
- 注意：按 `freq_phupha` 降序排序查询会 statement timeout（视图无索引），故排序必须在前端内存做。

### bug 修复
- `src/lib/db/search.js` 的 `scoreSearchResult`：原 `freq_ttc ?? freq_tnc ?? freq_phupha` 取「第一个非 null 列」会永远先取最小的 ttc 列导致失真 → 改为 `Math.max(freq_tnc, freq_ttc, freq_phupha)`，三列全 0/null 才视为无词频（`-Infinity`，走匹配度排序）。

### 构建与推送
- `npm run build` 通过。代码+memory 同一次 `git add -A + commit + push origin main`（不双推）。

## 中文搜索词频排序 + 排序丢弃 bug 修复

### 用户疑问
- 中文搜索也要按结果中的泰语词频排序；担心会不会很慢。

### 排查结论
- `search_words_zh` RPC（mig 03 版）`returns setof dictionary_full_ext` + `select *`，**已带 freq_tnc/freq_ttc/freq_phupha 三列词频**，中文结果本就有泰语词频数据。
- 发现 bug：`searchWords` 第153行 `sortSearchResults(merged,q)` 返回值未接住，排序结果被丢弃 → 泰文/中文搜索结果均未按词频排（返回 DB 原始顺序）。

### 修复
- `src/lib/db/search.js`：改为 `const sorted = sortSearchResults(merged, q)` 再 map，排序生效（泰文+中文统一按三列最大值词频降序，无词频走匹配度）。

### 性能结论（回应"会不会很慢"）
- 不会。词频列已随同一次 `SELECT *`/RPC 返回，无额外 DB 往返；排序在前端内存对 ≤40~80 行结果做 `Array.sort`，微秒级。DB 端按视图词频排序才会 statement timeout（已规避）。

### 构建与推送
- `npm run build` 通过。代码+memory 同一次 `git add -A + commit + push origin main`（不双推）。
