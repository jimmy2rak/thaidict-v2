# 2026-07-15 工作日志

- 【首页每日推荐不显示 + 400 错误修复】
  - F12 报 `user_checkin_completions` 和 `user_sentence_bookmarks` 400，根因：旧版 `02-sync-existing.sql` 漏建了这两张表，也漏建了 `get_random_word()` / `get_random_sentence()` RPC。
  - 修改 `supabase/migrations/02-sync-existing.sql`：追加 F/G 两段，补建 `user_sentence_bookmarks`、`user_checkin_completions` 及 RLS policy，补建两个随机取 RPC。
  - 新增 `supabase/migrations/04-fix-missing-after-02.sql`：仅含上述 4 项缺失补丁，方便已跑过 02 的用户直接单独执行。
  - 修改 `src/lib/db/daily-picks.js`：`loadDailyPick` / `refreshDailyPick` 的 fallback 在 RPC 不存在时自动改为直接查 `dictionary_full` / `sentences` 表随机取，避免首页空白。
  - 已本地提交 `8a6f7f3`；沙箱网络拦截 GitHub push，待用户本机 `git push origin main`。

- 【把当前账号设为 super_admin】
  - 用 service_role key 直连 Supabase，对 `user_id=8d1b33fb-5054-492a-a1d8-0888f5407ca8` upsert `user_roles` 表：`role='super_admin'`, `permissions=['all']`。
  - 已生效；刷新「我的」页面后会多出「用户权限管理」和「审批中心」两个入口。

- 【词源标签映射（用户给 7 表映射表）】
  - 新增 `src/lib/sourceMeta.js`：`SRC→{abbr, fullName, description}` 共 7 项（etcc计科/icu国标/orst皇院/th通用/thai2fit分词/volubilis外来/wikipedia百科），导出 `getSourceMeta`。
  - 新增 `src/components/SourceTag.jsx`：显示「二字简称」标签，点击后 `position:fixed` 气泡（标题=数据源全称，内容=详细词源&用途说明），点外部关闭；未知表名回退显示原值。
  - `WordDetailPage.jsx`：词头卡片 `meta.sources` 原裸表名 Badge → `SourceTag`；词典信息「来源明细」行改为 `SourceTag` 列表；义项 `source` 若为已知词源键也用 `SourceTag`（非 ai 其他值仍裸 Badge）。
  - `SentenceDetailView.jsx`：标题卡「来源」badge，若为已知 `src_words_xxx` 渲染 `SourceTag`，否则保持「来源 {source}」；句子表当前 source 多为空，属前瞻兼容。
  - 数据库实测：`dictionary_full.sources` = `['src_words_th','src_words_thai2fit','src_words_orst','src_words_volubilis','src_words_icu','src_words_wikipedia']`（另含未在样本出现的 `src_words_etcc`）；义项级 `source` 仅 `'ai'`（已隐藏）；句子 `source` 样本全空。
  - `npm run build` 通过；已 commit `33a1e93`，push 被沙箱网络拦截，待用户本机 `git push origin main` 触发 Vercel。
