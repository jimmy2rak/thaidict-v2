# 2026-07-15 工作日志

- 【AI 词条生成"拉不出数据"根因彻底修复 + DeepSeek 切换（commit 已推）】参照用户 `~/Downloads/05_enrich_parallel.py`（DeepSeek 批量富化脚本）修复：
  - **根因1（最关键）**：前端 `src/lib/ai-proxy.js` 的 `callAiProxy({word,zhHint})` 把【对象】当 prompt 传给 Edge Function，Edge Function 把它原样放进 AI `messages[].content`（必须是字符串）→ AI 收到 `[object Object]` → 生成失败。修复：新增 `buildWordPrompt(word,zhHint)` 生成 Python 风格详细字符串 prompt（显式要求一词多义/每义项专属例句/分词 segmented/近义 synonyms/反义 antonyms/学习者联想 learner_associations），`callAiProxy` 里对象自动转字符串。字符串 prompt（学习计划）原样透传。
  - **根因2**：免费 MiniCPM key（lis_sk_）对 4.6-V 系列 `lis_route_denied` 无路由（实测三模型仅 MiniCPM-o-4.5 通）。改用 py 文件里的 **DeepSeek** key，直接 PATCH `system_config` 三行：`SYSTEM_AI_API_KEY=sk-b9f40e0bea044e6ea114908f832c3aa6` / `SYSTEM_AI_BASE_URL=https://api.deepseek.com/v1`（注意带 /v1，Edge Function 拼 `${base}/chat/completions`）/ `SYSTEM_AI_MODEL=deepseek-chat`。实测 system 路径 ~9s、user 路径 ~14s 均返回**完整词条**（含 3 义项+分词），Supabase Edge Function 无 Vercel 10s 限制不超时。Edge Function 有 5min config 缓存。
  - **审批入库 bug（之前必 400）**：`dictionary` 基表 `sense_count` 是**生成列**(不可写)、且**无 `freq_ttc` 列**(词频在 `dictionary_full` 视图里从 `word_freqs` 映射)。`src/lib/db/search.js` 的 `normalizeDictionaryRow` 去掉这两列；`addDictionaryWord` 的 upsert 加 `onConflict:'word'`(word 有唯一约束，实测可用)。实测写 dictionary→dictionary_full 视图立即反映、sense_count 自动生成=查词直接命中。
  - `src/lib/utils.js` `normalizeRel` 兼容 AI 的 `{word, zh}`（原只读 `.meaning`，会丢中文）。
  - `src/context/AppContext.jsx`：`generatedWords` 持久化到 `localStorage('thaidict-generated-words')`（本地缓存，审批前刷新仍可查）；`handleGenerated` 校验 `data.parseError/!data.word` 并加成功/失败 toast。
  - 线上表结构探测法：PostgREST 不暴露 information_schema，用 `?select=<col>&limit=1` 逐列探，200=存在/400=不存在。
  - ⚠️ 词条详情渲染字段：例句 `ex.th`（兼容 thai）、`ex.zh`；近反义 `s.word`+异步 `getWordMeanings`；学习联想 `la.word`/`la.words`。AI 输出结构与之完全兼容。

- 【首页每日推荐不显示 + 400 错误修复】
  - F12 报 `user_checkin_completions` 和 `user_sentence_bookmarks` 400，根因：旧版 `02-sync-existing.sql` 漏建了这两张表，也漏建了 `get_random_word()` / `get_random_sentence()` RPC。
  - 修改 `supabase/migrations/02-sync-existing.sql`：追加 F/G 两段，补建 `user_sentence_bookmarks`、`user_checkin_completions` 及 RLS policy，补建两个随机取 RPC。
  - 新增 `supabase/migrations/04-fix-missing-after-02.sql`：仅含上述 4 项缺失补丁，方便已跑过 02 的用户直接单独执行。
  - 修改 `src/lib/db/daily-picks.js`：`loadDailyPick` / `refreshDailyPick` 的 fallback 在 RPC 不存在时自动改为直接查 `dictionary_full` / `sentences` 表随机取，避免首页空白。
  - 已本地提交 `8a6f7f3`；沙箱网络拦截 GitHub push，待用户本机 `git push origin main`。

- 【把当前账号设为 super_admin】
  - 用 service_role key 直连 Supabase，对 `user_id=8d1b33fb-5054-492a-a1d8-0888f5407ca8` upsert `user_roles` 表：`role='super_admin'`, `permissions=['all']`。
  - 已生效；刷新「我的」页面后会多出「用户权限管理」和「审批中心」两个入口。

- 【词源标签映射（用户给 7 表映射表）】
  - 新增 `src/lib/sourceMeta.js`：`SRC→{abbr, fullName, description}` 共 7 项（etcc计科/icu国标/orst皇院/th通用/thai2fit分词/volubilis外来/wikipedia百科），导出 `getSourceMeta`。
  - 新增 `src/components/SourceTag.jsx`：显示「二字简称」标签，点击后 `position:fixed` 气泡（标题=数据源全称，内容=详细词源&用途说明），点外部关闭；未知表名回退显示原值。
  - `WordDetailPage.jsx`：词头卡片 `meta.sources` 原裸表名 Badge → `SourceTag`；词典信息「来源明细」行改为 `SourceTag` 列表；义项 `source` 若为已知词源键也用 `SourceTag`（非 ai 其他值仍裸 Badge）。
  - `SentenceDetailView.jsx`：标题卡「来源」badge，若为已知 `src_words_xxx` 渲染 `SourceTag`，否则保持「来源 {source}」；句子表当前 source 多为空，属前瞻兼容。
  - 数据库实测：`dictionary_full.sources` = `['src_words_th','src_words_thai2fit','src_words_orst','src_words_volubilis','src_words_icu','src_words_wikipedia']`（另含未在样本出现的 `src_words_etcc`）；义项级 `source` 仅 `'ai'`（已隐藏）；句子 `source` 样本全空。
  - `npm run build` 通过；已 commit `33a1e93`，push 被沙箱网络拦截，待用户本机 `git push origin main` 触发 Vercel。

- 【修复 F12 三处报错（user_checkin_completions 400 / user_recent_words 409 / user_settings 400）】
  - 用 service_role 直连探测线上三表真实结构：
    - `user_settings` 是旧版建表，缺多列（speech_rate/chinese_font/font_size/thai_font/reminder_*/webdav_*/default_api_id/updated_at 等）→ upsert 报 400 "Could not find the 'xxx' column"。
    - `user_recent_words` 列正常（id/user_id/word/looked_up_at/lookup_count，唯一约束 user_id+word）；409 是并发查同词竞态（StrictMode 双重 effect / 快速点击）导致 insert 唯一冲突。
    - `user_checkin_completions` 表存在；`getMonthlyCheckinStreak` 用 `.like('date','2026-07%')` 对 date 类型列非法 → 400。
  - 代码修复：
    - `src/lib/db/checkin.js`：`getMonthlyCheckinStreak` 改用 `gte('date', 月首).lt('date', 下月首)` 范围查询，规避 date 列 like 非法。
    - `src/lib/db/recent.js`：`recordWordLookup` 在 insert 分支捕获唯一冲突(23505/duplicate)，冲突时回退为「读取当前计数+1」的 update，消除 409 红字。
  - 新增 `supabase/migrations/05-add-user-settings-columns.sql`：`ALTER TABLE user_settings ADD COLUMN IF NOT EXISTS` 补齐 14 列（幂等），消除 upsert 400。**需用户在 Supabase SQL Editor 执行该文件**（JS 客户端无法跑 DDL）。
  - `npm run build` 通过；已 commit+push `b3ce22b`，Vercel 自动部署。

- 【OAuth 登录回跳老网站排查（纯配置，无代码改动）】
  - 现象：Google/GitHub 登录完成后回跳到老项目网站。
  - 结论：代码正确——`src/lib/db/auth.js` 的 `redirectBase()` 优先取 `NEXT_PUBLIC_SITE_URL`(=https://thaidict.182183.xyz)，`signInWithOAuth` 已传新域名 redirectTo。
  - 根因：Supabase 项目(ref zvemahqskgluhirzbcqu) **Authentication → URL Configuration** 里 Site URL 仍是老域名，且新域名未进 Redirect URLs 白名单；Supabase 校验 redirect_to 未命中白名单时 fallback 到 Site URL(老站)。
  - 修复：控制台把 Site URL 改 `https://thaidict.182183.xyz`；Redirect URLs 加 `https://thaidict.182183.xyz/**` 和 `https://thaidict-jimmywang.vercel.app/**`；Save 后清缓存/无痕重测。Google/GitHub 的 callback(`ref.supabase.co/auth/v1/callback`)无需改。
  - 备忘：本地 localhost 测试也会回跳线上域名(因 SITE_URL 写死)，要测本地需临时改 SITE_URL 为 localhost 并加入白名单。

- 【OAuth 回调 TypeError: getSessionFromUrl is not a function 修复】
  - 现象：不再跳老站(白名单已配好)，但回调时 F12 报 `s.O.auth.getSessionFromUrl is not a function`，登录卡住。
  - 根因：`src/context/AppContext.jsx` 的 `handleAuthCallback` 用了 supabase-js **v1** 已删除的 `getSessionFromUrl()`；项目用的是 v2。
  - 修复：改用 v2 API——PKCE 流(OAuth `?code=`)用 `exchangeCodeForSession(code)`；隐式流(hash `#access_token=&refresh_token=`)手动解析 hash 后 `setSession({access_token,refresh_token})`；带 error 参数则清 URL；全程 try/catch。
  - `npm run build` 通过；commit+push `cf29b4c`，Vercel 自动部署。

- 【已有库结构漂移导致 400（user_checkin_completions 等）】
  - 现象：登录成功后 F12 报 `user_checkin_completions?select=date&...&date=gte...&date=lt...` 400。
  - 根因（用 service_role 直连 REST 复现确认 `42703: column user_checkin_completions.date does not exist`）：本库是**已有库**，02/04 全用 `create table if not exists`，老项目建过的同名表被跳过、保留旧结构。实测真实列：
    - `user_checkin_completions` 仅 `id/user_id/task_id/completed_at`（无 `date`/`completed`，老表用 `completed_at`）
    - `user_checkin_tasks` 缺 `name`/`plan_days`（创建任务会 400，尚未触发）
    - `user_recent_words` 仅 `id/user_id/word`（缺 `looked_up_at`/`lookup_count`，查词会 400，尚未触发）
    - `user_sentence_bookmarks`/`user_word_folders`/`user_word_folder_items` 结构一致，无需动；`user_settings` 因 05 已含新列。
  - 解决：新建 `supabase/migrations/06-sync-existing-alter.sql`——`alter table add column if not exists` 补齐三表到代码期望结构 + 回填 date(从 completed_at) + 去重 + 建唯一索引 `uniq_checkin_completions(user_id,task_id,date)`。纯幂等 DDL，不动老数据主体。
  - **需用户在 Supabase SQL Editor 手动执行 06**（JS 客户端无法跑 DDL，无 mgmt key）。commit+push `f2f73fc`。
  - 经验：今后新增/改表结构一律走「ALTER 补列」迁移而非依赖旧 `if not exists`，已有库才会真正生效。

- 【新建打卡任务 400：task_name not-null】
  - 现象：新建学习任务提示"已添加"但不显示，F12：`user_checkin_tasks?select=*` 400，报错 `null value in column "task_name" violates not-null constraint`。
  - 根因：用 OpenAPI（`/rest/v1/`）查清真实表列——老表真正主名称列是 `task_name`（NOT NULL），还有 `schedule_days/is_custom/updated_at`；06 误加了一个空的 `name` 列。代码写 `name` → 老 `task_name` 为 NULL → 触发 not-null。
  - 修复：新建 `supabase/migrations/06b-merge-task-name.sql`——DO 块判断（task_name+name 并存则并回 name 后 DROP task_name；仅 task_name 则 RENAME→name），再 `ALTER name SET NOT NULL`。幂等。commit `598c66f`。**仍需用户在 SQL Editor 手动执行 06b**。
  - logo：换主 logo 为 Downloads/未命名.svg（圆形徽标+泰文"พจนานุกรมจีน-ไทย"）。复制到 public/icons/logo.svg + 覆盖 icon-192/512.svg；CulturalIcons 新增 `MainLogo`(img)，LoginPage 用它替换 WatArunLogo；layout.jsx 加 apple-touch-icon。favicon/manifest/PWA 同步，已随 598c66f 部署。

- 【用户反馈：详情页例句泰文中文混行溢出；审批入库后搜不到词】
  - 现象：例句 `ex.th` 和 `ex.zh` 在同一行 flex 里，泰文长时中文被挤到右侧竖排；用户批准 `เล่มงาม` 入库后搜索仍只有谚语 `ช้า ๆ ได้พร้าสองเล่มงาม`。
  - 根因：
    1. 例句布局 CSS 问题：`WordDetailPage.jsx` 把 `ThaiSentence` 和中文 span 放在同一行 flex 容器。
    2. **审批实际没入库**：用 service_role 查 `dictionary` 和 `dictionary_full_ext` 均无 `เล่มงาม`；anon 写 `dictionary` 报 `42501 new row violates row-level security policy`（RLS 已启用但无 authenticated INSERT/UPDATE 策略）。前端 `onApprove` 没有检查 `addDictionaryWord` 返回值，toast 仍显示「已批准入库」，用户误以为成功。
    3. `saveCommunityWord` 也会失败：它把 `entry` 全部字段（含 `romanization/synonyms/antonyms/learner_associations/updated_at`）upsert，但真实 `community_words` 只有 `word/senses/zh_hint/source/created_at` 列。
  - 修复：
    - `WordDetailPage.jsx`：例句改为 `flex-direction:column`——第一行泰文、第二行中文，朗读按钮仍居右。
    - `src/lib/db/community.js`：`saveCommunityWord` 只提交真实库存在的列 `word/senses/zh_hint/source`。
    - `ApprovalCenterSection.jsx`：`onApprove` 检查 `saveCommunityWord`/`addDictionaryWord` 返回值，失败时 toast 具体错误。
    - 新增 `supabase/migrations/08-fix-dictionary-rls.sql`：为 `dictionary` 表添加 `authenticated_insert_dictionary` 与 `authenticated_update_dictionary` 策略（RLS 已启用，需用户在 SQL Editor 执行该文件）。
  - 应急：已用 service_role 手动把 `เล่มงาม` 写入 `dictionary` 基表，现在 `dictionary_full_ext` 已能精确匹配，用户刷新后搜索即可看到。
  - commit 已 push，Vercel 自动部署。


## 五合一修复（commit 0c0b6ff，已推送 Vercel 部署）
用户提 5 项问题，全部在代码层修复：
1. **设置**：`src/lib/db/settings.js` 默认中文字体 `noto_serif_sc`、泰语 `sarabun`；`SettingsSection.jsx` 移除"翻译方向"UI（删 DIRS/seg/dict_direction state 与 Card），`ProfilePage.jsx` desc 同步去掉翻译方向。
2. **查词 400**：根因 `search.js` 的 `or('...senses::text.ilike...')` 在 jsonb 列上 PostgREST 解析失败（400，沙箱侧 fetch 还被代理拦成 500）。实测 `.or()` 整体在代理层被拦，`.eq()`/`.ilike()` 单独用正常。改 `searchWords` 用 `dictionary_full_ext` 的 `eq('word')`+`ilike('word')` 分别查（不经 or），中文检索走已验证可用的 `search_words_zh` RPC；`searchCommunityWords` 去掉 jsonb 强转改用 `ilike('word')`。
3. **user_sentences 结构漂移（已有库）**：OpenAPI 探明真实列=`id/sentence_th(NOT NULL)/sentence_zh/related_words/text[]/source/status/submitter_id/created_at`，代码原写 `submitted_by/thai/zh/category` 全错。改 `sentences.js`：`getSentencesByCategory`/`getSentenceById`/`getUserSentencesList` 的 `submitted_by`→`submitter_id`，且 user_sentences 无 category 列故去掉其分类过滤；`normalizeSentence` 兼容 `sentence_th/sentence_zh`；`addUserSentence` 映射真实列。修复了"短语库打开 400：`user_sentences.submitted_by does not exist`"。
4. **单词书 BookTab 缺失**：`WordBookPage.jsx` 第57行引用未定义的 `<BookTab>`，新增 `BookTab` 组件（调 `getWordBooks` 列单词书卡片）并修正调用（去掉 userId 形参）。
5. **收藏进句子夹 + 句子卡统一**：`bookmarkSentence` 收藏后同步进默认"我的收藏"句子夹（`user_folders` 查建 + `addSentenceToFolder`），解决"句子收藏后句子夹不出现"；首页「每日一句」与单词本「句子夹」卡片统一用 `PhraseCard` 并加 `showCategory`，复刻短语库卡片形式（去掉首页多余的 Card 嵌套 wrapper）。
- 经验：Supabase 代理会拦 `or=` 参数（或返回 500/400），凡涉及多条件过滤优先拆成 `.eq`/`.ilike` 多次查询或走 RPC；jsonb 列不要在前端 filter 里 `::text` 强转。

## 四合一修复（commit 2ff2008，已推送 Vercel 部署）
1. **单词本卡片复用 WordCard**：新建 `src/components/WordCard.jsx`（泰词大字+罗马音+释义+例句+朗读+收藏(toggle)+可选刷新），与「每日一词」同结构。首页 `DailyWordCard` 改用它；`WordBookPage` 的 `RecentTab`/`WordFolderDetail` 改用它，并通过 `getWordByThai`+`transformWordData` 拉取释义丰富显示（最近项带「查 N 次」副标题）。
2. **收藏可取消 + 句子详情提示选夹**：`sentences.js` 新增 `removeSentenceBookmark`（删 user_sentence_bookmarks 并从默认「我的收藏」句子夹移除）；`SentenceDetailView` 点星未收藏时 `openFolder()` 弹出句子夹选择、已收藏时 `removeSentenceBookmark` 取消；`PhrasesSection.handleBookmark` 与首页 `DailySentenceCard.toggleBookmark` 均改为 toggle（可取消）。修复"外面卡片不能收藏/所有星星不能取消/句子详情收藏不提示选夹"。
3. **底部菜单深层级返回**：`App.jsx` 四个页面 wrapper 加 `key={app.page}`，切换 tab 时整页重挂，重置 `WordBookPage` 等内部深层级 state（原组件常驻不卸载导致点 tab 仍停在文件夹详情）。
4. **放大 logo / favicon**：`LoginPage` 主 logo `MainLogo size=88` 单独成一行（原 inline-flex size52 旁有文字）；`public/icons/logo.svg`、`icon-192.svg`、`icon-512.svg` viewBox 由 `0 0 600 600` 收紧为 `134 110 330 330`，去掉大量留白使 favicon 图标更大更清晰。
- 经验：跨页面内部导航状态无法靠 app.setPage(resetNav) 重置，因为页面在 App 中常驻挂载；最稳的办法是给页面 wrapper 加 `key={page}` 强制重挂。

## 本轮追加（当前会话）

- 【修正·四合一修复中「深层级返回」其实没真正修好】
  - 复查发现上轮给四个页面 wrapper 加的是**常量 key**（`key="home"` 等）+ `display` 切换常驻挂载，常量 key 不会触发重挂载，`WordBookPage` 的 `detail` 状态在切 tab 时仍持续存在。
  - 正确做法：`src/context/AppContext.jsx` 加 `pageEpoch` state；`setPage` 每次调用都自增；`src/App.jsx` 改为单活动页渲染，用 `key={`${app.page}-${app.pageEpoch}`}` 让每次切 tab 整页重挂载。同时覆盖「切到别 tab 再回来」和「点已激活同 tab 重置」两种场景。已 commit `b22fa9c` push。

- 【system_config 内置 AI API 代理落地】
  - 用户要求：把 `system_config` 表里的系统默认 AI API 作为前端默认内置 API，前端不明文接触 key，用 API 时走数据库/服务端。
  - 实现：新建 `app/api/ai/route.js`（Next.js Route Handler），用 `getServerSupabase()` service_role 读 `system_config`（默认 key `ai_system_api`，可配置 `SYSTEM_AI_CONFIG_KEY`），解析 `provider/api_key/base_url/model`，调用 OpenAI 兼容 `/chat/completions`；支持用户自带 `userApi` 覆盖。返回结果尝试 JSON.parse 方便前端 `transformCommunityWord` 消费。
  - 修改 `src/lib/ai-proxy.js`：mock 模式不变；真实模式从「调 Supabase Edge Function `ai-proxy`」改为「调 `/api/ai`」。密钥不再到前端。
  - `.env.example` 新增 `SYSTEM_AI_CONFIG_KEY` 说明。
  - `npm run build` 通过；commit `6f0fa4d` 已 push，Vercel 自动部署。
  - 待用户确认：真实 `system_config` 中 AI 配置的 key 名和 JSON 字段名是否与默认假设一致（默认 key `ai_system_api`；JSON 字段 `provider/api_key/base_url/model`）。若不一致，改 `.env.local` 的 `SYSTEM_AI_CONFIG_KEY` 或改 route 字段映射即可。
  - 待用户补完：「先把 mindsoya」后半句意图（目前截图显示 mindsoya 已是超级管理员，未明确要做什么）。

- 【用户权限管理菜单（超管专用）+ DB 操作指南】
  - 用户需求：①把 mindsoya@gmail.com 设为超管；②模仿截图界面做「用户权限管理」菜单，仅超管可见；③超管可在菜单里选成员提升为管理员；④超管本身只能 DB 手动设，菜单内加「数据库操作指南」把相关 SQL 放出来。
  - **关键阻断 bug（已修）**：`user_roles` 的 RLS 是 `own_roles`（`user_id = auth.uid()`），前端 `user_roles.select('*')` 只能读到调用者自己那一行→超管看不到其他成员、也改不了别人角色。所以必须走 service_role 服务端代理。
  - 新增 `app/api/admin/users/route.js`（GET）：`requireSuperAdmin`（anon client 解析 Bearer→auth.uid()→service_role 读其 role 校验 super_admin）→ `server.auth.admin.listUsers` 拉全量用户邮箱/注册时间，join `user_roles` 的 role/permissions 返回。
  - 新增 `app/api/admin/set-role/route.js`（POST）：同样校验 super_admin；`upsert user_roles`；**硬性拒绝 role==='super_admin'**（保证超管只能 DB 手动设）。
  - 新增 `src/lib/db/adminApi.js`：`fetchMembers`/`adminSetRole`，真实模式 fetch 上述路由（带 Authorization），mock 模式回退 roles.js 的 listUsers/setUserRole。
  - 改造 `src/screens/subsections/AdminManagementSection.jsx`：改用 fetchMembers/adminSetRole；顶部加可折叠「数据库操作指南（超级管理员）」卡片，内含 3 段可复制 SQL（①设为超管 ②取消超管 ③DB 直接提升管理员），SQL 自动 `email→auth.users.id` 解析 UUID、幂等。超管行保持 UI 不可编辑提示。
  - `ProfilePage.jsx` 第39-41 行「用户权限管理」入口本就 `isSuperAdmin` 门禁，无需改。
  - `npm run build` 通过（新增 2 个 ƒ 动态路由）；commit `79e6f7d` 已 push，Vercel 自动部署。
  - **mindsoya@gmail.com 设为超管**：本环境无 Supabase DB 直连（service_role 仅能走 REST/RPC，不能跑任意 DDL/SQL），故该步需用户在 Supabase SQL Editor 手动执行（SQL 已内嵌菜单指南，幂等）。脚本同下：`insert into user_roles ... select id,'super_admin',array[]::text[],now() from auth.users where email='mindsoya@gmail.com' on conflict(user_id) do update set role='super_admin'...`。

- 【系统内置 AI API 前端卡片（打码 + 鉴权编辑）】
  - 用户指出：之前 `/api/ai` 只是服务端调用了 `system_config`，但**前端没有可视/可编辑卡片**来呈现这个内置默认 AI API；要求前端给一张「内置 AI API」卡片（密钥打码不明文），仅超管或拥有权限的管理员可编辑。
  - `roles.js` 的 `PERMISSION_OPTIONS` 新增 `manage_system_ai: '管理系统内置 AI API'`（超管可在「用户权限管理」里授予普通管理员此权限）。
  - 新增 `src/lib/serverAdminAuth.js`：`getCallerRoleRow(authHeader)`（anon 解析 Bearer→auth.uid()→service_role 读其 role/permissions）+ `canManageSystemAi(row)`（super_admin 或 admin 且含 manage_system_ai）。
  - 新增 `app/api/admin/system-ai/route.js`：`GET` 任意登录用户可读，返回**打码**配置（provider/base_url/model + keySet/keyMasked，密钥 `sk-****` 形态，不到前端明文）；`POST` 仅 `canManageSystemAi` 可写，写 `system_config`（默认 key `ai_system_api`，可配 `SYSTEM_AI_CONFIG_KEY`），key 留空则保留原值。
  - `system_config` 仅有 `enable row level security` 无 policy（默认拒绝），故前端/普通客户端都不能直连读写，全部走 service_role 代理，符合「前端不明文」。
  - 新增 `src/lib/db/systemAiApi.js`：`fetchSystemAi`/`updateSystemAi`（真实模式 fetch 上述路由带 Authorization；mock 模式返回默认/无操作）。
  - 改造 `ApiKeysSection.jsx`：顶部加「系统内置 AI API（默认）」卡片——显示服务商/模型/base_url、密钥状态打码；编辑按钮仅 `isSuperAdmin||hasPermission(manage_system_ai)` 可见，否则显示锁图标 +「仅超级管理员或被授权管理员可编辑」。编辑表单 Base URL/模型/API Key（placeholder 提示留空保留），保存走 POST。
  - `npm run build` 通过（新增 ƒ 动态路由 /api/admin/system-ai）；commit `42cc99a` 已 push，Vercel 自动部署。

- 【AI 调用全部上线：用户可选默认 API，未选回退系统内置；学习计划真正调用 AI；system_config 诊断增强】
  - 用户反馈：①「系统内置 AI API」显示未配置密钥；②要求所有 AI 功能（查词查不到时 AI 生成词条、制定学习计划）全部接入 /api/ai，且用户可在 API 列表中选择当前要使用的 API，不选则默认调用内置。
  - 诊断：截图显示「OpenAI · — · 未配置密钥」，说明 GET 已回退到默认值但读不到 api_key。增强 `app/api/admin/system-ai/route.js` 与 `app/api/ai/route.js` 的 `resolveConfig/readKey`，额外兼容 `token/apiToken/secret/api_secret` 等常见字段名；GET 返回 `notFound` / `hint` 诊断信息，前端卡片显示具体原因（如"配置存在但缺少可识别的 api_key..."或"未找到 key=ai_system_api"）。
  - 用户默认 API 选择：`src/lib/db/api-keys.js` 新增 `getActiveAiApi(userId)`：读 `user_settings.default_api_id` → 取 `user_api_keys` 对应行 → 返回 `{key,base_url,model,provider,name}`；未设默认返回 null。
  - 查词查不到 AI 生成词条：`AppContext.handleGenerated` 先 `getActiveAiApi(userId)`，将结果作为 `userApi` 传给 `callAiProxy`；未选默认时 `callAiProxy` 回退 `system_config` 系统内置。
  - 制定学习计划：`AdjustPlanSection.generateAI` 从本地写死推荐改为真正调用 AI：构造泰语学习计划 prompt（要求返回 JSON 数组，含现有任务避免重复）→ `callAiProxy(prompt, userApi)` → 解析并 setAiPreview；新增 `aiLoading` 状态与按钮 loading 样式。
  - 修改 `ApiKeysSection.jsx`：系统内置 AI 卡片显示诊断提示（hint）。
  - `npm run build` 通过；commit `8cf4aa2` 已 push，Vercel 自动部署。
  - 修复：将 `system_config` 内置 AI API 读取方式从单 key `ai_system_api` 改为用户实际表结构的三行扁平键：`SYSTEM_AI_API_KEY`、`SYSTEM_AI_BASE_URL`、`SYSTEM_AI_MODEL`。`app/api/admin/system-ai/route.js` 与 `app/api/ai/route.js` 均改为 `.in('key', ...)` 读取三行；管理端 POST 只写三行（base_url/model/key，留空保留原值）。`ApiKeysSection` 编辑系统内置 API 时去掉 provider 选择（系统实际无该字段）。`.env.example` 中将 `SYSTEM_AI_CONFIG_KEY` 环境变量标记为弃用，改为用表内三行。commit `b4ff520` 已 push，Vercel 自动部署。用户后续应直接按真实字段名在 `system_config` 中维护三行配置。

- 【修复 AI 生成词条 502 HTML 错误】
  - 现象：前端生成词条时浏览器 network `POST /api/ai 502 (Bad Gateway)`，前端 `ai-proxy.js` 第 29 行 `await res.json()` 报 `SyntaxError: Unexpected token '<', "<!DOCTYPE "`。
  - 根因：502 是 Vercel 把**未捕获异常**包装成的 HTML 错误页（非我们 route 返回的 JSON）。原 `/api/ai/route.js` 的**数据库读取段 `supabase.from('system_config').in(...)` 不在 try/catch 内**——Vercel 上冷启动/网络抖动导致 supabase client reject 时，异常漏到 handler 外 → Vercel 吐 HTML 502。
  - 修复：重写 `app/api/ai/route.js`，**整段 POST 包一层大 try/catch**，数据库读取与上游 fetch 各自再包 try/catch，上游响应先 `res.text()` 再安全 `JSON.parse`（上游返回 HTML 错误页也不崩）；任何异常都 `return NextResponse.json({error}, {status:500/502})`，绝不再让 Vercel 返回 HTML。并加了 console.error 日志（请求 URL、上游 status、body 前 500 字符）便于 Vercel 日志排查。
  - `npm run build` 通过；commit `d107bc7` 已 push，Vercel 自动部署。
  - 后续若仍失败，前端会直接显示 route 返回的 JSON `error` 文本（如"AI 服务网络异常：..."或上游具体 message），据此可精确定位是 Supabase 读取失败还是 ModelBest 上游拒绝。

- 【AI 生成词条 502 根因定位（最终结论）】
  - 用户反馈仍 502。用无头方式实测线上（node fetch）：`GET /`=200、`GET /api/ai`=405(Vercel 正常)、`POST /api/verify-otp`=400 JSON(正常)、`POST /api/ai 空prompt`=400 JSON(`prompt 不能为空`，证明函数可完整执行)、`POST /api/ai 非空prompt`=502 Cloudflare 页。
  - **探针验证**：在 `/api/ai` 加临时分支——读完 system_config 后、调用大模型前直接 return 配置摘要（不调 ModelBest）。实测返回 200 JSON：`{probe:true, hasKey:true, baseUrl:"https://api.modelbest.cn/v1", model:"MiniCPM-V-4.6-Thinking", userApi:false}`。**证明函数与 Supabase 读取完全正常，502 唯一来源是调用 ModelBest 大模型推理时函数被平台 kill**。
  - **最终根因**：用户的 Vercel 是 **Hobby 计划，函数硬超时 10 秒**（route 里 `export const maxDuration=60` 在 Hobby 被压回 10s，故 55s AbortController 未触发即被 kill）。`MiniCPM-V-4.6-Thinking` 推理耗时 >10s → 函数被 kill → Cloudflare 显示 502。
  - 已做的代码优化（在 Hobby 下均不生效，仅为 Pro 准备）：`maxDuration=60`、`fetch` 加 AbortController 55s 主动超时、Supabase 读取 `withTimeout` 10s、payload 加 `max_tokens:800`。
  - **待用户决策的方案**：①升级 Vercel 到 Pro（让 maxDuration=60 生效，代码已就绪，升完即恢复；Pro 可配到 300s 更保险）；②把 system_config 的 `SYSTEM_AI_MODEL` 换成 ModelBest 上更快的模型（代码不动，改配置）；③改异步架构（新增 Supabase 任务表 + worker/Cron，前端轮询，彻底绕开函数超时，改动大）；④暂用用户自己在「AI API 密钥」里配的更快 key 并设为默认，前端走用户 key 绕开内置慢模型。
  - 实测手段沉淀：对线上 API 502，用 `node fetch` 直接打线上 URL 即可（如 `https://thaidict.182183.xyz/api/ai`），比依赖前端 F12 更快定位是 Vercel 函数崩溃还是 Cloudflare/平台层拦截；Cloudflare 的 502 页 title 为 `182183.xyz | 502: Bad gateway`，可借此区分"Vercel 函数返回 5xx"与"Cloudflare 自身拦截"。

- 【AI 代理根因最终定位 + 根治（改回 Supabase Edge Function）】
  - 用户提醒：老系统（github.com/jimmy2rak/thaidict，Vite SPA + `vercel.json` 纯静态无 function）之前能用同款大模型。clone 老仓库分析：`src/lib/supabase.js` 的 `callAiProxy` 走 **Supabase Edge Function `${supabaseUrl}/functions/v1/ai-proxy`**（Deno），`supabase/functions/ai-proxy/index.ts` 读 system_config 的 `SYSTEM_AI_API_KEY/BASE_URL/MODEL` 三行（与新系统一致），支持 `user_api_key` 覆盖，含完善 JSON 解析与中泰词典 system prompt。
  - **结论**：新系统把 AI 代理从老系统的 Edge Function 改成了 Vercel function（`/api/ai`），撞上 Vercel Hobby 10s 硬超时（MiniCPM 推理 >10s 被 kill → Cloudflare 502）。老系统因走 Edge Function 不受此限制。
  - **根治**：`src/lib/ai-proxy.js` 改回调用 Supabase Edge Function `${supabase.supabaseUrl}/functions/v1/ai-proxy`（带 Bearer 登录 token，body 传 `user_api_key/user_base_url/user_model/provider`），返回 `{success,data,provider}` 适配为 `{data,error}`。删除无用的 `app/api/ai/route.js`（Vercel function）。commit `891b1ab`（改回 Edge Function）+ `62fd189`（清理无用 function）。
  - **无头验证 Edge Function（关键）**：直接 `POST https://zvemahqskgluhirzbcqu.supabase.co/functions/v1/ai-proxy`（不带登录 token，让 Edge Function 用 service_role 读 system_config 的内置 key 调 ModelBest）。结果 `STATUS 502 JSON` + `{"error":"AI API request failed","details":"...invalid_api_key...API 密钥无效或已停用..."}`。
  - **最终真相**：①超时问题已根治（Edge Function 可调用、读配置、调 ModelBest 全通，不再有 Vercel 10s 限制）；②**当前真正拦路的是 `system_config` 的 `SYSTEM_AI_API_KEY` 值本身无效/已停用**（ModelBest 返回 invalid_api_key）。"老系统之前能用"说明当时 key 有效，现在 key 过期/被停用。
  - **待用户操作**：去 ModelBest 拿有效 key，在 Supabase 的 `system_config` 表更新 `SYSTEM_AI_API_KEY` 的值（或用超管账号进「我的 → AI API 密钥」编辑系统内置 API填入有效 key——此前做的 system-ai 编辑功能正是为此）。key 有效后 AI 生成词条/学习计划即可正常工作（链路已验证通畅）。
  - 经验：①Vercel Hobby 函数 10s 硬超时无法用代码突破，大模型推理类需求应走 Supabase Edge Function（或升 Pro）。②老系统能用的功能，排查时优先 clone 老仓库看其真实实现，往往就是答案（避免"重写反而引入平台限制"）。

- 【AI key 格式诊断：lis_sk_ 前缀不符合 ModelBest 规则】
  - 用户提供了"官方给的 key"：`lis_sk_298cf78155f231c7_DkrDcNLHnK8dJRnfFrJCd4JGDbBLMkHrC3T-wLpvC9zy0BPemsyFuQ`，问是否符合规则。
  - 实测：用该 key 直连 ModelBest 多个端点——`api.modelbest.cn/v1/chat/completions`、`api.modelbest.cn/chat/completions`、`api.modelbest.cn/openai/v1/chat/completions`——**全部返回 `401 invalid_api_key / API 密钥无效或已停用`**；`modelbest.cn/v1/...` 是 404（端点不存在，非 key 问题）。说明 key 本身 ModelBest 不认，与 base_url 无关。
  - 关键规则：ModelBest/面壁官方 API key 是 **`sk-` 开头**（参见其平台文档/控制台生成的令牌），几乎所有 OpenAI 兼容平台（硅基流动、阿里云百炼、中转聚合 API）也都是 `sk-` 开头（顶多有 `sk-sp-` 之类变体）。用户这个 `lis_sk_` 前缀**不符合 ModelBest 官方规则**。
  - 推断：`lis_sk_` 前缀是**第三方中转/聚合平台或子账户令牌**的特征，需配合该平台自己的 base_url 才能用，`api.modelbest.cn` 下无效。老系统当时能用，说明当时 system_config 存的是有效的 `sk-` 开头 key（或某个可用的中转 key），与现在这个 `lis_sk_` 不是同一个。
  - **用户给的 `lis_sk_...` 其实是 MiniCPM-V 官方免费公开 key（非"不符合规则"）**：用户发来 OpenBMB/MiniCPM-V 官方 api.md（https://github.com/OpenBMB/MiniCPM-V/blob/main/docs/api.md），文档明确：Base URL = **`https://api.modelbest.co/v1`**（`.co` 不是 `.cn`），免费公开 key 正是 `lis_sk_298cf78155f231c7_DkrDcNLHnK8dJRnfFrJCd4JGDbBLMkHrC3T-wLpvC9zy0BPemsyFuQ`，可用模型 `MiniCPM-V-4.6-Instruct` / `MiniCPM-V-4.6-Thinking`。
  - **实测对比（决定性）**：打 `api.modelbest.cn`（`.cn`，即 system_config 当前配的）→ 该 key 报 `401 invalid_api_key`（域名错，key 不被识别）；打 `api.modelbest.co`（`.co`，文档规定的）→ key 被识别（**不再 invalid_api_key**），但所有模型名均返回 `404 lis_route_denied / 无法根据 model 定位推理服务`。
  - **结论**：①key 本身没问题，根因是 system_config 的 `SYSTEM_AI_BASE_URL` 配成了 `.cn`（应改 `.co`）。②但即便改对，该免费 key 的后端推理服务当前返回路由拒绝（`lis_route_denied`），很可能 ModelBest 已停掉/维护这个免费服务，或该 key 权限已过期——这是 ModelBest 服务端状态，我们改不了。
  - **修复动作**：把 `SYSTEM_AI_BASE_URL` 改为 `https://api.modelbest.co/v1`（SQL：`update system_config set value='https://api.modelbest.co/v1' where key='SYSTEM_AI_BASE_URL';` 或超管 UI 编辑系统内置 API）。改后若仍 404，则需去 modelbest.cn 官网申请正式 key 替换 `SYSTEM_AI_API_KEY`，或等官方恢复免费服务。

- 【AI 502 最终根治：免费 key 只路由 MiniCPM-o-4.5，不路由 4.6-V 系列】
  - 用户已把 `SYSTEM_AI_BASE_URL` 改对（`.co`），但 F12 仍 `POST /functions/v1/ai-proxy` 502。用 service_role 直连 REST 读 system_config 确认三行正确：`BASE_URL=https://api.modelbest.co/v1`、`MODEL=MiniCPM-V-4.6-Instruct`、`API_KEY=lis_sk_...`。
  - **直连 ModelBest（绕过 Edge Function）用文档 free key 实测三个模型**：
    - `MiniCPM-V-4.6-Instruct` → 404 `lis_route_denied`「无法根据 model 定位推理服务」
    - `MiniCPM-V-4.6-Thinking` → 409 `lis_route_denied`「推理服务尚未处于可调用状态」
    - `MiniCPM-o-4.5` → **200 正常返回 completion**
  - **结论**：免费公开 key `lis_sk_...` 只能路由 `MiniCPM-o-4.5`，**4.6-V 系列在该 key 下无路由**（所以 502 一直来自 ModelBest 的 `lis_route_denied` 被 Edge Function 原样转成 502，与超时/代码/域名无关）。`SYSTEM_AI_MODEL=MiniCPM-V-4.6-Instruct` 正是 502 真凶。
  - **修复**：`PATCH system_config set value='MiniCPM-o-4.5' where key='SYSTEM_AI_MODEL'`（service_role REST，HTTP 204 成功）。Edge Function 运行时读 system_config（5 分钟缓存），无需重部署。
  - **端到端验证**：①user-override 路径强塞 `MiniCPM-o-4.5` → `{"success":true,"data":{"word":"สวัสดี",...}}`；②默认 system 路径紧接着也返回 200 成功（缓存已换）。AI 生成词条/学习计划 502 彻底解决。
  - 注：若用户想用 4.6-V 系列，须去 modelbest.cn 申请带该模型路由的正式 key 替换 `SYSTEM_AI_API_KEY`（免费 key 不行）。
  - 环境备忘：本机 `supabase` npm CLI 在 darwin-arm64 下报 `No matching Supabase CLI binary package found`（损坏），验证改走 REST + service_role key 直连，可用。

- 【用户添加自有 AI Key（DeepSeek）失败 400：`user_api_keys` 结构漂移】
  - 现象：F12 `POST /rest/v1/user_api_keys?select=*` 400，`[saveApiKey] Could not find the 'base_url' column`。顺带 ai-proxy 仍 502/500（用户尝试用自己的 DeepSeek key 走 user 路径，key 没存进去/配置不对所致）。
  - 真因（用 `select=<col>&limit=1` 逐列探测确认）：线上 `user_api_keys` 真实列 = `id, user_id, provider, api_key, model, created_at`。
    - 缺 `name` / `base_url` / `is_active` 三列；
    - 密钥列名是老的 `api_key`，而新代码（api-keys.js / ApiKeysSection.jsx）读写的是 `key`。
    - 根因同前：已有库，`02/00` 的 `CREATE TABLE IF NOT EXISTS` 跳过老表，老结构（含 `api_key` 列名）从未被升级。
  - 逻辑上系统内置 AI（MiniCPM-o-4.5）**已可用**：单独测 `POST /functions/v1/ai-proxy` provider=system 返回 200 真实词条。用户报"还是不行"是其 DeepSeek 尝试走 user 路径失败，非系统路径。
  - 修复（新建 `supabase/migrations/07-fix-user-api-keys.sql`，**需用户在 Supabase SQL Editor 手动执行**，REST/JS 不能跑 DDL）：
    1. `ADD COLUMN IF NOT EXISTS name text NOT NULL DEFAULT ''` / `base_url text DEFAULT ''` / `is_active boolean DEFAULT true`；
    2. `DO $$` 块：若 `api_key` 存在且 `key` 不存在 → `RENAME COLUMN api_key TO key`（保留数据）；
    3. 补 `user_update_api_keys` UPDATE 策略（老表只有 SELECT/INSERT/DELETE，编辑密钥 upsert 的 UPDATE 会被 RLS 拦）；
    4. `ENABLE ROW LEVEL SECURITY` 幂等确认。
  - 用户跑完需验证：①ApiKeysSection 能保存 DeepSeek（base_url=`https://api.deepseek.com/v1`，model=`deepseek-chat`，key=`sk-...`）；②设为默认后 `getActiveAiApi` 返回该配置，edge function 走 user 路径调 DeepSeek。注意 UI 模型默认 `gpt-4o`，用 DeepSeek 须改成 `deepseek-chat`。

