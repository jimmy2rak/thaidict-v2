# 2026-07-13

## 修复：AppProvider 运行时 TDZ 报错
- 现象：`ReferenceError: Cannot access 'userId' before initialization`，浏览器打开 http://localhost:3000 白屏。
- 根因：`src/context/AppContext.jsx` 中两个 `useEffect`（加载角色权限、加载用户设置）的依赖数组用了 `userId`，但 `const userId` 声明在文件靠后（原第173行）。初始渲染执行到 `useEffect(..., [userId])` 时 `userId` 处于暂时性死区（TDZ）。
- 修复：把 `const userId = session?.user?.id || null` 与 `const isLoggedIn = !!session` 提前到 `session`/`loading` 声明之后，删除原靠后重复声明。
- 教训：在 React 函数组件里，被 `useEffect` / `useCallback` 依赖数组或同步渲染逻辑引用的派生 `const`，必须声明在所有 hooks 调用之前（hooks 调用即求值依赖数组）。
- 已 `git commit`（fix: 修复 AppProvider 中 userId TDZ 报错），开发服务器重启在 localhost:3000 正常（HTTP 200）。

## 5 项新需求实现（同日本次第二轮）
- 需求 #1 文件夹计数恒为 0：`mockFolders` 读 `getUserColl(userId,'folder_words')`（按用户），但 `addWordToFolder`/`getFolderWords` 写/读的是全局键 `'__'`，统计键名不一致 → 改为读 `'__'`。
- 需求 #2 「我的」页面无法滚动：App.jsx 四个页面外层 div 是 `display:block;height:100%`，内部 `flex:1` 无法获得约束高度，`scroll-y` 不生效；改为 `display:flex;flexDirection:column;height:100%`。WebDAV 新增「一键上传/一键导出」+ 类别选择（学习日记/我的笔记/学习统计），新增 `src/lib/webdav.js`（downloadJson + uploadToWebdav PUT + saveLocalBackup 回退）。
- 需求 #3 搜索 AI 按钮：HomePage `SearchResults` 后追加「未找到词语？AI 搜索」→ `navigateTo({type:'unknown',word})`；`search.js` 新增 `addDictionaryWord`（mock→dictionary / real→dictionary_full，标准格式）；审批中心 `onApprove` 批准后除写 community_words 外自动加入主词典。
- 需求 #4 箭头假死：`UIComponents.IconButton` 原本**不支持 disabled**（忽略该 prop）→ 加上 disabled 置灰+禁点击；`AppContext.goBack` 在 `navStack` 为空时 `setDetailWord(null)` 关闭浮层返回上一页。
- 需求 #5 词条详情增强：`search.js` 新增 `getWordMeanings(word)`（词典+社区词库，返回含义数组或 null）；一词多义义项加序号；例句加朗读按钮；近反义词/学习者建议单词后接中文括号（多义分号分隔，查不到不显示）。新增 `RelWord` 组件。

## 一键启动脚本 + 文档完善
- 用户反馈 localhost:3000 打不开：排查发现端口未被占用，纯粹是 dev server 没启动。
- 新增 `启动中泰词典.command`（zsh，已 chmod +x）：自动 cd 脚本目录、把托管版 Node（`~/.workbuddy/binaries/node/versions/22.22.2/bin`）加入 PATH、缺 node_modules 时 `npm install`、释放 3000 端口旧进程、`npm run dev`、后台轮询端口就绪后 `open` 浏览器；退出时 `read` 停留显示日志。小白友好的中文分步提示 + 颜色输出。已验证 `npm run dev` HTTP 200。
- 新增 `todo.md`（项目根，小白向）：项目说明、两种运行方式、进度总览表（阶段1-6）、已完成功能明细、待办、已知问题、避坑提醒。
- 新增 `.workbuddy/memory/MEMORY.md`（项目长期记忆，小白向）：项目定位、运行方式、技术栈、目录结构、核心架构约定（数据层单一入口 + mock/真实自动切换）、工具函数速查、踩坑教训、Git/部署现状、下一步。
- 约定：用户偏好 `.command` 启动文件而非 `.sh`。
- 已 `git commit`（feat: 修复文件夹计数/滚动/箭头 + 搜索AI按钮 + 词条详情增强 + WebDAV上传导出）。`npm run build` 通过（2299 modules），localhost:3000 HTTP 200。

## 泰语 newmm 分词 + 例句点击查词气泡（纯前端，无后端）
- 用户原计划要 Next.js App Router + Edge Function 的 `app/api/thaiToken/route.js`，但中途改主意：**不走 Next.js / 不搭后端**，直接在现有 Vite 项目里用**纯前端 TS 分词函数 + LocalStorage 缓存**完成（保持项目现状，两栈不混）。
- 新增 `src/utils/thaiToken.ts`（**项目首个 .ts 文件**，Vite/esbuild 原生支持，无需 tsconfig）：复刻 PyThaiNLP newmm 正向最长匹配；含 normalize 清洗、泰语字符连续段整体合并（前引字/双辅音/堆叠元音天然不分断）、内置基础词库 `BUILTIN`、自定义词库 `addCustomWord/addCustomWords`（持久化到 localStorage）、`simpleSyllableSplit` 兜底、`tokenizeFull` 返回 `{origin,cleanText,wordList,joinText}`；前端客户端 `getTokens(text)` 提供**永久 localStorage 缓存（key=原文）+ 300ms 防抖复用 + 异常降级（主分词→粗分→原句不拆分）**。
- 新增 `src/components/WordBubble.jsx`：悬浮气泡弹窗，展示「查询中…/释义列表/暂无该词条释义」，点外部关闭。
- 新增 `src/components/ThaiSentence.jsx`：例句渲染组件，props `text/type('sentence'|'word')/separator(默认'+')/lookup(默认 getWordMeanings)/onWordClick`；分词结果遍历渲染，`<u>` 下划线 + `+` 分隔；点击单词弹气泡查词；标点单独成 token 且点击不触发；已接入 `WordDetailPage` 例句（`ex.thai` 改为 `<ThaiSentence type="sentence">`，保留中文释义与朗读按钮）。
- 单测（esbuild 转译后 node 跑）：`สวัสดีค่ะ ฉันชอบหมา`→`สวัสดี+ค่ะ+ฉัน+ชอบ+หมา`；复合词/前引字/标点分离/自定义词库均正确。`npm run build` 通过（2302 modules），dev server HTTP 200。
- 已 `git commit`（feat: 泰语 newmm 分词 + 例句点击查词气泡（纯前端，无后端））。
