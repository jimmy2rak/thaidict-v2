# 2026-07-14 工作日志

- 调整计划页（AdjustPlanSection.jsx）按用户给的新结构完整重构：
  - 顶部导航（返回+标题「调整学习计划」）→ 今日数据卡（日期/已完成 0/1 + 🔥连续天数）→ 已有任务条目（圆形○+名称+类型Badge+分钟+重复日+×删除，点击整卡进入编辑）→ 分割线「添加新任务」→ 新增/编辑表单（学习类型多选 chips+自定义、任务名称、圆形重复日一二三四五六日、预计时长 5/10/15/20/30/45/60 → 灰色填充「添加任务」）→ 分割线「AI推荐」→ AI智能推荐卡（AI生成计划深棕填充/导入外部计划浅线框/复制Prompt到外部AI细边框；生成后内联预览可「应用推荐计划」；导入为 JSON 文本域）。
  - 打卡任务 `task_type` 单值升级为 `task_types` 数组（多选）。新增 `src/lib/taskTypes.js` 常量；`src/lib/db/checkin.js` 的 create/update 支持 `task_types` 并向后兼容；LearnPage 打卡项类型显示改用 `typeLabels()`。
  - 已 headless（Edge）冒烟：0 JS 错误，添加任务/AI预览均正常。
- 早前（本会话）根因修复：佛学短语未分词 → `thaiToken.ts` 兜底从「整段吞成一个 token」改为「逐字符扫描」+ 扩充词库 + 缓存前缀 v1→v2；词条详情序号/词性标签改深棕主题色 `--c-primary`。`npm run build` 通过。
- 全局配色克制化（用户要求）：
  - 降级荧光感：teal #7FBF6E→#8FA98C(鼠尾草绿)、info #5FA3C2→#6E8CA0(雾霭蓝灰)、amber #E0B040→#CBA14A(暗琥珀)；保留 primary/gold/rose 不变（调整计划、一词多义依赖）。
  - 单词本上方 4 个分类 Tab 激活态→主题棕 `--c-primary`（原 teal）。
  - 学习中心「今日打卡」进度环/完成圈/「一键完成」按钮 + 近7天热力条 + MiniStat「已打卡」均由 teal 绿改为 `--c-primary` 棕；学习中心 Dumbbell 图标同步改棕。
  - 词条详情：近义词(teal绿)/反义词(rose陶土)/学习者建议(gold金) 标签随全局降饱和；3 个操作按钮重配色——加入文件夹=棕 `--c-primary`、词形分析=蓝灰 `--c-info`、笔记=陶土红 `--c-rose`；文件夹弹层「+」按钮改棕；新建文件夹默认色 #9FB08E→#8FA98C。
  - headless 冒烟验证 0 JS 错误；计算样式确认 Tab激活/进度环=`rgb(166,138,91)` 棕、3 按钮=棕/蓝灰/陶土红。
- git：已 push 到 GitHub `jimmy2rak/thaidict-v2`（main 跟踪 origin/main）。之前本地提交：a65ba84 阶段5补强 / c599ae4 调整计划重构 / 4d63598 配色克制化 / c44a2ec 数据库映射 / d351143 Brevo后端。
- Brevo 邮件后端落地：原 auth.js 真实模式调不存在的 Supabase Edge Function，改为 Next.js Route Handler（app/api/send-otp、app/api/verify-otp）+ src/lib/brevo.js（sendBrevoEmail）+ src/lib/supabaseServer.js（service_role）。auth.js 改写调 /api/*。这样 Brevo 三项密钥能进 Vercel 的 .env.local 一键导入。env 模板 .env.example + scripts/import-env.sh 已加。待用户给密钥填 .env.local 并导入 Vercel。
- Supabase 数据库映射（用户指令：扫描文档、严禁改库内容、缺口补前端）：
  - 读 `docs/rebuild/01-项目基础信息与锁定清单.md`（20 表 + 7 RPC）。反向提取 `src/lib/db/*.js` 实际列名/类型/onConflict（探索代理盘点 26 实体）。
  - 产出 `supabase/migrations/01-create-schema.sql`：20 文档表 + 9 新表(成就/角色/审批/日记/练习/单词书) + 8 RPC + RLS + 唯一约束 + 索引。纯 DDL，无数据写入。
  - 产出 `supabase/DATABASE_MAPPING.md`：文档表→新系统 db 文件+前端页+缺口结论。
  - 缺口结论：**文档 20 表前端入口新系统已基本全覆盖**（仅 `system_config` 后端only、`dictionary` 旧表并入 `dictionary_full` 不需前端）→ **无需新增前端入口**。新系统反是超集。软缺口：`user_sentence_bookmarks` 无独立"我的收藏句子"列表页（句子夹已覆盖），未做，待确认。
  - 代码对齐：`user_learning_plans` 表设 `daily_minutes` 列 + `plan` JSONB；`saveLearningPlan` 改为 `upsert({user_id, daily_minutes, plan})` 对齐 LearnPage 读 `plan?.daily_minutes`。`npm run build` 通过。已 commit c44a2ec。
  - 提示用户：文档写 Vite 的 `VITE_SUPABASE_*`；新系统用 Next.js 的 `NEXT_PUBLIC_SUPABASE_*`（已在 `src/lib/supabase.js` 实现，无需改）。
- 已收到用户重新生成的全部密钥，写入 `.env.local`（gitignored，密钥未进 GitHub）：
  - 修正：用户贴的 `NEXT_PUBLIC_SUPABASE_URL` 带 `/rest/v1/` 后缀 → 已自动清理为 `https://zvemahqskgluhirzbcqu.supabase.co`（不带后缀，否则 `.from()`/auth 拼接出错）。
  - Supabase 项目 ref `zvemahqskgluhirzbcqu`；站点域名 `thaidict.182183.xyz`（自定义）；Brevo 发信 `noreply@thaidict.182183.xyz` / 发信名「智能中泰词典」。
  - 沙箱禁外网出网（google 也 000），无法 live 校验密钥；URL 与 anon key JWT `ref` 声明一致、Brevo key 格式合法，结构正确。待 Vercel 导入 env 后由真实环境验证。
  - 下一步：用户需 (1) Supabase SQL Editor 跑 `01-create-schema.sql` + `00-fix-known-bugs.sql`；(2) Vercel 导入 `.env.local`；(3) Vercel 关联 GitHub `jimmy2rak/thaidict-v2` 部署。
- 【重大修正·数据库映射】用户贴出真实库 schema（31 张表）后发现：
  - 词库在 `dictionary` 表（62,101 行），但新代码原查 `dictionary_full`（该表在用户库不存在！文档曾误写 `dictionary_full` 为规范名，实际用户命名为 `dictionary`）。
  - 已统一：代码（search.js 7处 + AppContext.jsx 1处）+ 01-create-schema.sql + DATABASE_MAPPING.md + 注释 全部 `dictionary_full`→`dictionary`；并把 synonyms/antonyms 类型对齐为真实 `text[]`（旧 01 误写 jsonb）。`dictionary.senses`/`learner_associations` 为 jsonb，与代码对得上。零空间、直接复用 6 万词。
  - 用户库已存在的表（共 31）：community_words/daily_picks/sentences/otp_codes + 大量 src_words_*/word_freqs/word_sources/wordnet/system_config/user_api_keys/user_bookmarks/user_checkin_*/user_folder_*/user_folders/user_learning_*/user_notes/user_recent_words/user_sentence_bookmarks/user_sentences/user_settings/user_submissions。
  - **缺失需新建的 9 张**：pending_approvals/user_roles/user_achievements/user_diaries/user_diary_images/user_practice_records/user_practice_wrong/word_books/user_word_book_progress。
  - 已写 `supabase/migrations/02-sync-existing.sql`（差量迁移，严禁改数据）：只 CREATE 缺失 9 表+RLS；已有表仅 ADD COLUMN IF NOT EXISTS（user_notes.tags、user_checkin_tasks.task_types/task_type、user_learning_plans.daily_minutes/plan）；确保 dictionary/sentences/daily_picks/community_words 匿名可读 RLS；CREATE OR REPLACE `search_words_zh` RPC；otp_codes 仅 enable RLS（service_role 写）。
  - ⚠️ 待确认：otp_codes 真实列结构（verify-otp 路由写入 email/code/purpose/expires_at，需与用户表吻合）；以及 user_api_keys/user_settings 等写入列是否与代码一致。已请用户贴全部表列结构（一条 information_schema 查询）以最终校验。
  - 上线顺序修正：阶段6 跑 `02-sync-existing.sql`（已有库）或 `01-create-schema.sql`（全新库），再 `00-fix-known-bugs.sql`，再导入 env。

- 【再次重大修正·dictionary_full 实为视图】用户澄清：`dictionary_full` 在他真实库里**是已存在的视图**（Supabase 表编辑器"小眼睛"=视图图标），本身不存数据，只综合映射 `dictionary` 主表 + `word_freqs` 词频表 + `word_sources` 来源表成每词一行。我早前误判它不存在、错改成查基表 `dictionary`，现已**撤销并改回查 `dictionary_full`**。
  - 读/写分工：**读取**（查词 searchWords、getWordByThai、getDictionaryCount、getWordMeanings、AppContext 分词词典加载）一律走 `dictionary_full` 视图；**写入**（审批入库 addDictionaryWord）仍落基表 `dictionary`（视图自动反映；视图不可直接写，会报错）。
  - 代码改动：`src/lib/db/search.js`（exact/fuzzy/getWordByThai/count/meanings 5 处查 `dictionary_full`；addDictionaryWord 的 upsert 保持 `dictionary` 基表）；`src/context/AppContext.jsx`（loadDictFromDB 改 `dictionary_full`）。`npm run build` 通过。
  - 迁移脚本修正（**绝不重建/覆盖 dictionary_full 视图**）：
    - `01-create-schema.sql`：注记澄清视图本质；仅在全空库时用 `DO $$ ... CREATE VIEW IF NOT EXISTS dictionary_full AS SELECT * FROM dictionary`（已存在则跳过，不覆盖）；`search_words_zh`/`get_random_word` RPC 改 `returns setof dictionary_full` + `from dictionary_full`；§5.1 给 `dictionary_full` 视图补 anon/authenticated 公开读 RLS + 策略。
    - `02-sync-existing.sql`：新增 `dictionary_full` 公开读 RLS + 策略（只补权限，不改定义）；`search_words_zh` RPC 改查视图。
    - 新建 `03-extend-dictionary-full.sql`：把用户新增词（community_words）以 `UNION ALL` 并入视图的**安全模板**，须先用 `pg_get_viewdef('dictionary_full')` 拿到真实视图定义定稿，列数/列序/类型须与视图逐一对应，绝不盲改。
  - `DATABASE_MAPPING.md` 更新：#1/#2 改为 `dictionary_full`(读取视图)/`dictionary`(基表)；新增 §3.4 说明用户新增词/句并入映射的方案（词→视图 UNION 可选；句→`sentences.js` 查询层 UNION，待确认用户句子落哪张表）。
  - 待用户确认：(1) 在 SQL Editor 跑 §0 的 3 条查询贴回 `dictionary_full` 列清单+视图定义+community_words 列清单，以便定稿 03；(2) 用户新增【句子】具体落哪张表（现有 `sentences`？还是另有 `user_sentences`？）以补 `sentences.js` 合并查询。
- 定稿 03（用户已贴回真实 schema）：
  - `dictionary_full` 16 列（id bigint / word / romanization / romanization_source / senses jsonb / sense_count int / synonyms ARRAY / antonyms ARRAY / learner_associations jsonb / enrichment_status / origin / sources ARRAY / freq_tnc/ttc/phupha bigint / user_sentence_count bigint）；视图定义 SELECT 自 dictionary d，LEFT JOIN word_sources/word_freqs/user_sentences(approved) 算 freq 与 user_sentence_count。
  - `community_words` 真实列：id uuid / word / romanization / senses jsonb / synonyms jsonb / antonyms jsonb / learner_associations jsonb / submitted_by uuid / source text / zh_hint text / created_at；**无 status 列**。
  - 03 改为新建 superset 视图 `dictionary_full_ext` = dictionary_full UNION ALL community_words；id 统一 text（社区词加 `cw_` 前缀），synonyms/antonyms jsonb→text[]（兼容字符串与 {word} 对象数组），community 行 enrichment_status/origin='community'；视图级 `grant select ... to anon, authenticated`；RPC search_words_zh 改查 ext。绝不重建 dictionary_full。
  - 修正 01/02 重大 bug：曾对视图 dictionary_full 写 `alter table ... enable row level security` + policy——PostgreSQL 不允许对视图启用 RLS，会报错。改为底层基表(dictionary/word_freqs/word_sources/user_sentences) anon 可读策略 + 视图级 grant select。
  - search.js 四个读取函数优先查 dictionary_full_ext，视图未创建时回退 dictionary_full+community_words 双查询（部署安全）。
  - 待确认：`user_sentences` 属主列——用户说"id uuid"（应为 PK），代码按 `user_id` 写（sentences.js 4 处 `.eq('user_id', userId)`）。若真实列为 submitted_by/created_by 需改。

- 【澄清·01 vs 02 安全性（用户担心 01 清数据）】：核对 01/02 全文——两者**均无 `drop table`/`truncate`/`delete`**，纯 DDL，跑它们不会动 62101 词。但 **01 绝不可在已有库跑**：其 `create policy "public_read_dictionary"` 等多数**无 `if not exists`**，会与现有策略冲突报错、留半成品；02 全用 `if not exists` + `create or replace` + `add column if not exists`，对已有库零风险。所谓"自增"仅新表 PK 用 `bigserial`/`gen_random_uuid()`，与 `dictionary` 无关；脚本无 seed/INSERT 数据。已把 MEMORY.md「上线顺序」改为按库状态二选一，并强调已有库跑 02→03→00。
- 【用户问能否迁移 Turso】已答：能，但 `dictionary_full` 视图(jsonb/text[]/array_agg 子查询)、`auth.users` 外键、RLS 三处是 Postgres 专属，搬 Turso(libSQL/SQLite) 须重写为应用层 join + 自带鉴权 + 服务端注入 `where user_id=?`。建议先 02→03→00 用着 Supabase，迁移另立项。待用户选 A(出上线执行手册) 或 B(出 Turso 迁移方案)。
- 【选 A：出小白上线手册】用户选 A。修正 `00-fix-known-bugs.sql`：`user_api_keys` 3 条策略原裸 `CREATE POLICY`（无 IF NOT EXISTS），在已存在该表的库上会报错中断；改为 `DROP POLICY IF EXISTS` + `CREATE POLICY` 安全写法，使 00 可重复跑。新建 `supabase/DEPLOY_MANUAL.md`（小白版：02→03→00 复制粘贴 Run + Vercel 导入 env 部署 + 自查清单 + 排错锦囊；醒目标注**绝不可跑 01**）。`npm` 无需 build（纯 SQL/doc）。已 commit c963e79 push。注：Write 时误把「开发者」拼成 `developer`，已 `mv` 修正到正确中文路径并 `rm -rf` 误建目录。

- 【用户新增句子方案落地】用户确认走**独立 `user_sentences` 表**（与 `sentences` 同结构 + `user_id`）。已实施：
  - `src/lib/db/sentences.js`：新增 `getUserSentences`/`getUserSentencesList`(real: user_sentences 按 user_id)/`addUserSentence`(写入 user_sentences) 助手；`getSentencesByCategory(category, userId)` 与 `getSentenceById(id, userId)` 在查询层 `UNION` 合并 `sentences`(全局) + `user_sentences`(当前用户)，前端统一渲染（带 `origin: 'global'|'user'`）；mock 走 `getUserColl(userId,'user_sentences')`。
  - `src/screens/subsections/PhrasesSection.jsx`：`getSentencesByCategory` 传入 `userId`，依赖补 `userId`。
  - `db/index.js` 用 `export *` 已自动导出新助手。
  - `DATABASE_MAPPING.md` §3.4 改为"已实现"，并标注假设 `user_sentences` 属主列为 `user_id`（若实际是 `created_by` 等需改 `.eq('user_id', userId)`）。
  - `npm run build` 通过；已 commit 22a944f push。`user_sentences` 为真实库已有表（31 表清单之一），未新建。
  - 仍待用户贴 `dictionary_full` 的 §0 三查询以定稿 03（词视图 UNION community_words）。
- 【02 跑错修复·PG CREATE POLICY 不支持 IF NOT EXISTS】用户跑 02 报 `42601: syntax error at or near "not"`（LINE 22 `create policy if not exists "public_read_dictionary"`）。根因：PostgreSQL 的 `CREATE POLICY` **不支持 `IF NOT EXISTS` 子句**（我此前记忆/脚本误以为支持）。修复 `02-sync-existing.sql`：15 处 `create policy if not exists` 全部改为「先 `drop policy if exists ...` 再 `create policy`」安全写法；并给 02、03 的 `search_words_zh` RPC 加 `drop function if exists search_words_zh(text, int)` 前置（因 PG 不允许 `CREATE OR REPLACE` 改返回类型，02 建 `setof dictionary_full`、03 改 `setof dictionary_full_ext` 必须先用 DROP）。01 经核对已无 `create policy if not exists`（无需动）。同步更正 MEMORY.md 第 54 行错误记忆。待用户重跑 02→03→00。

- 【登录 bug 修复·magic link 回调用不上】用户反馈"能收到 magic link 但点了登录不了"，F12 见 `content_script...js startsWith`(浏览器扩展误报，非本应用) + `signup 422`/`token?grant_type=password 400`(登录页点密码/注册入口产生，与 magic link 无关) + `otp... 429`(Supabase 对 magic link 发送限流，需间隔几分钟重发)。根因：(1) `AppContext.jsx` 只处理 PKCE 的 `?code=` 回调，对 magic link 隐式流(`#access_token`/`type=magiclink`)完全没处理，仅依赖 supabase-js 默认 `detectSessionInUrl:true` 自动检测——该检测异步且竞态，组件挂载时未 set 进 React，表现为"没登录"；(2) 域名错位：magic link 回跳到 `thaidict-jimmywang.vercel.app`(Vercel 自动域名)而非 `thaidict.182183.xyz`，因 `sendMagicLink` 用 `window.location.origin`(发链接时人在 vercel.app)，而 Supabase 回调白名单没加 vercel.app，忽略回跳地址回退到 Site URL。修复：`supabase.js` 设 `auth.detectSessionInUrl:false`（改手动）；`AppContext.jsx` 新增 `handleAuthCallback()` 显式识别 hash 中 `access_token`/`type=magiclink|recovery|signup` 或 query `?code=`，调用 `supabase.auth.getSessionFromUrl()` 设 session 并 `history.replaceState` 清 token；`auth.js` 新增 `redirectBase()` 用 `NEXT_PUBLIC_SITE_URL || window.location.origin` 统一回跳域名（magic link 与 OAuth 一致），并修 OAuth 原回跳 `/auth/callback`(无该路由→404) 改回首页。`npm run build` 通过。

- 【短语库全屏"出错了"崩溃】用户点短语库后整页 ErrorBoundary 触发全屏"出错了"。排查：`PhraseCard`/`ThaiSentence`/`UIComponents` 对缺失字段均有容错，纯代码路径不会因字段缺失 throw；崩溃 100% 来自真实 `sentences` 表某条具体数据形态（用户 31 表清单未给 `sentences` 列结构，代码假设的 `thai/zh/category/segmented` 很可能不符）。修复（commit `f5a8174`）：① `sentences.js` 新增 `normalizeSentence()`，对 `getSentencesByCategory` 的 mock/real 两条返回路径做归一化——多候选字段名兜底（`thai←content/text/word`、`zh←translation/meaning/zh_hint`、`category←type/tag`），`segmented` 字符串→JSON 解析、非数组→undefined，缺失字段给默认值，类型校正为字符串/数组；② `PhrasesSection.jsx` 加 `CardBoundary` 单行错误边界包裹每个 `PhraseCard`，万一某条仍 throw 只隔离该条；③ 分类按钮由"读 mock localStorage"改为"基于已加载真实数据动态生成"。`npm run build` 通过、已 push。
  - **待用户补一步(精准字段对齐)**：在 Supabase SQL Editor 跑 `select * from sentences limit 2;` 把结果（列名+样例值）贴回，确认真实 `sentences` 表的泰文/中文/分类字段到底叫什么，必要时在 `normalizeSentence` 里补映射，确保卡片正确显示（当前已覆盖常见替用名，大概率能直接显示）。

- 【本轮修复·搜索点击崩溃 + 短语中文 + 登录页重做 + Brevo/OAuth 教程】
  - **搜索点击全屏"出错了"**：根因 `WordDetailPage.jsx` 第184行 `la.words.map(...)`——真实 `learner_associations` 条目里 `words` 字段可能不是数组（数据结构与假设不符），`.map()` 抛 "Cannot read properties of undefined (reading 'map')"，被顶层 ErrorBoundary 接住。修复：`(Array.isArray(la.words) ? la.words : (la.word ? [la.word] : [])).map(...)`；顺手给 `HomePage.jsx` SearchResults 的 `r.senses[0]` 加可选链 `r.senses?.[0]?.meaning`。
  - **短语库中文/字面/实际意义不显示**：`sentences.js` 的 `normalizeSentence()` 升级为「中文自动探测」——`pickChinese()` 扫描原始行所有含 CJK 的字段值兜底（兼容任意列名），`zh/literal/actual/advice/tags` 均加多候选名 + `asArray()` 处理字符串/数组；`getDailySentence()` 返回也走归一化。卡片 `{item.actual || item.zh}` 与详情页 `literal/actual` 现在能显示中文（即使真实列名不是 `zh/literal/actual`）。
  - **登录页重做**（按用户新结构）：`LoginPage.jsx` 重写为 —— Logo+介绍 → Google/GitHub 圆形按钮(内联 Google "G" SVG + lucide Github) → 细分割线("或") → Toggle 组[密码登录|验证码/链接登录] → 输入框 → "登录/注册"按钮(先试登录失败自动注册) → 小字协议提示 + 找回密码 → 底部淡色 GitHub 图标+"前往仓库"超链接(https://github.com/jimmy2rak/thaidict-v2)。OTP/链接子切换：验证码走 Brevo(`/api/send-otp`)、链接走 `sendMagicLink`。
  - **Brevo 邮件路由**：确认 OTP 验证码早已走 Brevo(`/api/send-otp`→`sendBrevoEmail`)；Magic Link 走 `supabase.auth.signInWithOtp`（Supabase 自带邮件器，故"未过 Brevo"）。正解=Supabase Auth→Email→Custom SMTP 指向 Brevo 中继(smtp-relay.brevo.com:587)，无需改代码。Google/GitHub OAuth 需在 Supabase Providers 启用并填 Client ID/Secret，且第三方回调必须填 `https://zvemahqskgluhirzbcqu.supabase.co/auth/v1/callback`。已写教程 `supabase/OAUTH_BREVO_SETUP.md`（含逐步步骤+验证清单+FAQ）。
  - 待 commit+push（用户本机推；沙箱网络曾抖）。


- 【晚·按真实 schema 精修字段映射（用 supabase-js 直连查回真实结构）】
  - 用 `.env.local` 的 service_role key 写 `query_schema.cjs` 直连 Supabase，查回真实列：
    - `sentences` 真实列：id / text(泰文) / category / literal_meaning(字面) / actual_meaning(实际) / learner_tip(学习者建议) / source / difficulty / tags[] / segmented(json字符串) / created_at。
    - `dictionary_full` 真实列：word / romanization / romanization_source / senses[{pos,source,meaning,examples[{th,zh}],register,sense_id}] / sense_count / synonyms[] / antonyms[] / learner_associations[{note,word}] / enrichment_status / origin / sources[] / freq_tnc/ttc/phupha / user_sentence_count。
  - 修复 `src/lib/db/sentences.js` `normalizeSentence`：**之前漏把 `learner_tip` 映射进 `advice`**（详情页"学习者建议"空白）；现按真实列名精确映射（text→thai、literal_meaning→literal、actual_meaning→actual、learner_tip→advice），保留未知 schema 的中文自动探测兜底。卡片中文走 `actual`(actual_meaning)。
  - `SentenceDetailView.jsx`：新增 source/difficulty Badge（标题卡下）。
  - `WordDetailPage.jsx`：按 `dictionary_full` 全字段展示——(1) 逐义项加 语体(register)/来源(source)/义项号(senseId) Badge；(2) 词头下加「罗马音来源」；(3) 操作按钮前新增「词典信息」卡：义项数/丰富状态/语料来源(origin)/来源明细(sources)/用户句子数/词频(TNC·TTC·Phupha)，全部从 `row`(=getWordByThai 返回的 dictionary_full 原行) 读取。
  - `npm run build` 通过；本地提交 `1ae550d`，push 被沙箱网络拦截(Empty reply from server)，待用户本机 `git push origin main` 触发 Vercel。

- 【晚·词条详情/每日推荐 UI 修复（用户截图反馈）】
  - `WordDetailPage.jsx`：
    - 词头区域改为大卡片区，包含 word（32px 泰文）、romanization、romanization_source、义项数 Badge、origin/sources Badge，右侧放大朗读按钮。
    - 义项例句字段修正：真实数据库为 `examples: [{th, zh}]`，代码原用 `ex.thai` 导致泰语例句不显示 → 改为 `ex.th ?? ex.thai`；朗读也用 `ex.th ?? ex.thai`。
    - 反义词拆出独立 Card，与近义词各自成卡。
  - `HomePage.jsx` `DailyWordCard`：例句显示同样改为 `first.examples[0].th ?? first.examples[0].thai`。
  - `daily-picks.js` + `sentences.js`：
    - 导出 `normalizeSentence`，`loadDailyPick` 对 `getSentenceById` 返回的原始 sentences 行做归一化，使 `DailySentenceCard`/`PhraseCard` 能正确显示 `.thai`/`.actual`。
    - `loadDailyPick` 增加兜底：daily_picks 空表时，直接用 `get_random_word` RPC + `getDailySentence` 随机取词/句，确保首页始终有每日推荐卡片。
    - `refreshDailyPick` 增加 Edge Function 失败后的前端随机回退，并尝试 upsert 回写 daily_picks（有写权限则持久化，否则本次展示）。
  - 已 commit+push `a327cd0` 到 GitHub，Vercel 自动部署中。

- 【晚·分词彻底修复（newmm 匹配 dictionary_full 真实词条，杜绝单字母拆分）】
  - 根因：AppContext 把真实词表只注入了 `thaiSegment.js`，而真正渲染句子分词的 `ThaiSentence.jsx` 用的是 `thaiToken.ts`（仅 ~300 内置词）→ 生词逐字符回退成单字母拆分。另：`getSentenceById` 返回原始行，`segmented` 仍是 JSON 字符串未解析。
  - `src/utils/thaiToken.ts`：
    - 新增 `setDictWords(words)` 把 `dictionary_full` 的 word 列合并进 DICT（与 BUILTIN/自定义词库去重）；新增 `getDictSize()`。
    - `tokenize` 重构：前向最长词典匹配；**匹配失败时不再逐字符回退**，而是把连续非空白/非标点段整体作为 `residual` 词块（仍在该段内尽力再切出最早的最长词典词），彻底杜绝单字母拆分。
    - `TokenType` 增加 `residual`；`simpleSyllableSplit` 兜底也改为整段保留。
    - 缓存前缀 `v2→v3`（废弃旧版单字母错误缓存）。
  - `src/components/ThaiSentence.jsx`：渲染改为「仅相邻两个 type==='word' 词块之间加分隔符」；residual/标点不加分隔符、不拆单字母、residual 不可点击。
  - `src/context/AppContext.jsx`：加载词表后**同时**调用 `thaiToken.setDictWords`（原先只调 thaiSegment）；且去掉 `if(!session)return` 守卫，未登录也加载词典（首页/每日推荐也能正确分词）。
  - `src/lib/db/sentences.js`：`getSentenceById` 真实模式返回前 `normalizeSentence`，确保 `segmented` 被解析为数组（按 id 直接打开详情时也能正确分词）。
  - 数据库 `sentences.segmented` 实为金标准分词 JSON：`[{"text","pos","meaning"}]`（如 `น้ำขึ้นให้รีบตัก`→5词）；有则优先用，无则走 newmm。
  - `npm run build` 通过；已 commit+push `5a3dcfd` 到 GitHub，Vercel 自动部署中。

- 【晚·按截图反馈修复（例句分词 / ai 标签 / 反义词卡片 / 每日推荐）】
  - `src/utils/thaiToken.ts`：
    - 新增 `DICT_VERSION` 版本号 + `subscribeDictVersion` 订阅机制；`setDictWords`/`addCustomWord`/`clearCustomWords` 在重建词典后调用 `bumpDictVersion()`，**自动清空旧分词缓存**并通知所有订阅者。
  - `src/components/ThaiSentence.jsx`：
    - 使用 `useSyncExternalStore` 订阅字典版本；版本变化时 `useEffect` 重跑分词。解决「真实词库异步注入前已缓存了单字母/residual 错误结果」导致的例句无下划线问题。
  - `src/screens/WordDetailPage.jsx`：
    - 义项卡片：隐藏 `source === 'ai'` 的标签；保留其他来源。
    - 学习者联想：原代码按 `learnerAssociations.map` 每对象一卡，导致出现多个单字卡片；改为**全部合并到一张「学习者联想」卡**，并去重。
  - `src/lib/db/daily-picks.js`：
    - `loadDailyPick` 重构为「配置的每日词/句查不到时，自动回退到 `get_random_word` / `getDailySentence` 随机取」，避免首页每日推荐卡片空白。
  - 已 commit `0c57ebf`；push 被沙箱网络拦截（Empty reply from server），待用户本机 `git push origin main` 触发 Vercel 自动部署。
